## SITUATION

Read these files before writing any code:
- `.claude/rules/01-nexus-finance-v1.md` — especially §0 (BUILT vs PLANNED), §1 (Signal Set B, B3 lands with 8b), §5 (thresholds, `AMOUNT_TOLERANCE` prose), §10 (tenant scoping), §11 (NOT-SCOPE), §13 (session guardrails)
- `CLAUDE.md` — test command, append-only log rules, feature-branch rule
- `core/matching/scoring.py` — the shipped 8a Signal Set B implementation you are extending in place
- `core/graph/entity_store.py` — the shipped query-helper module you are extending in place
- `db/schema_sqlite.sql` and `db/schema.sql` — the two dialects
- `db/migrations/002_llm_training_data.sql` and `db/migrations/002_llm_training_data_sqlite.sql` — migration file conventions (see NON-GOALS for the pattern NOT to copy)
- `tests/test_schema_parity.py` — the parity enforcement mechanism
- `tests/test_scoring.py` — existing B-signal test conventions and the `conn` fixture
- `connectors/base.py:68-85` — `NormalizedTransaction`, the shape the new table mirrors
- `core/ingestion/normalizer.py:62-72` — `NormalizedEntity`, source of the join-key fields

Skills: None.

**Current state (verified against the tree — these facts win over any recollection):**
- `core/matching/scoring.py:78` — `BoostEntry.signal_id: Literal["B1", "B2", "B4", "B5", "B6"]`. No `"B3"`.
- `core/matching/scoring.py:239` — second `Literal` on the local `raws` annotation: `raws: list[tuple[Literal["B1","B2","B4","B5","B6"], float]] = []`. No `"B3"`.
- `core/matching/scoring.py:236` — band gate: `if base_score < B_BOOST_BAND_LOW or base_score >= B_BOOST_BAND_HIGH: return ()`. Constants: `MAX_B_BOOST = 0.20` (`:124`), `B_BOOST_BAND_LOW = 0.70` (`:129`), `B_BOOST_BAND_HIGH = 0.90` (`:130`).
- `core/matching/scoring.py:299` — `if not raws: return ()`, before renormalization.
- `core/matching/scoring.py:303` — `scale = min(1.0, MAX_B_BOOST / total_raw) if total_raw > MAX_B_BOOST else 1.0`, applied at `:304-307`. This is **proportional renormalization, not a clamp**.
- `core/matching/scoring.py:222-223` — `_compute_b_boosts` signature ends with `person_count` / `neighbor_count: Optional[int] = None`.
- `core/matching/scoring.py:622-630` — `_weighted_score(base_score, b_boosts) -> float`.
- `core/matching/scoring.py:641` — `score_pair(..., source_canonical_id: Optional[str] = None)`; docstring at `:651` confirms the normal Stage 3 path leaves the entity unresolved.
- `core/matching/scoring.py:679-684` — `person_count` / `neighbor_count` are queried **unconditionally**, before `in_b_band`.
- `core/matching/scoring.py:688` — `in_b_band = B_BOOST_BAND_LOW <= base_score < B_BOOST_BAND_HIGH`; `candidate_ext` is gated on it at `:689-693`.
- `core/graph/entity_store.py` — module-level functions, parameterized SQL only, `tenant_id: Optional[str] = None` last, explicit `if tenant_id is None:` branch with two separate query strings (see `count_shared_person_neighbors` at `:446-485`, `:466-484`, and `count_shared_graph_neighbors` at `:488-501`). `count_amount_cooccurrence_periods` does **not** exist. `AMOUNT_TOLERANCE` is **not** a Python constant anywhere in the tree.
- `db/schema_sqlite.sql` has exactly 4 tables; no `transactions`. `db/schema.sql:72-83` `system_references` uses `UNIQUE (tenant_id, source, external_id)` at `:81`.
- `tests/test_schema_parity.py:26-31` `SHARED_TABLES` (4 entries); `:46-51` `EXCLUDE` dict with `canonical_entities: {"tenant_id"}` (`:47`), `system_references: {"tenant_id"}` (`:50`), `entity_aliases` / `entity_edges`: `set()`. `test_column_parity` subscripts `EXCLUDE[table]` at `:121-122` with **no `.get()` and no default**.
- `tests/test_scoring.py:62-64` — the `conn` fixture runs `executescript()` over `db/schema_sqlite.sql`, so adding `transactions` to the base SQLite schema makes it available to seeded-row tests with no fixture change.
- `tests/test_scoring.py:886` — `test_b_boosts_total_applied_capped_at_max` exists and pins the 4-signal case.
- No shipped test enforces the 8a "B3 must be absent" guard; that guard lives only in build artifacts under `features/`. Adding B3 breaks nothing.
- `models/cc.en.300-compress.bin` exists.
- Fixture-mode `read_transactions` returns `[]` in both connectors (`connectors/quickbooks.py:272-274`, `connectors/ruddr.py:203-205`) — B3 is therefore dark outside tests, which is accepted and intended.
- Nothing in the acceptance criteria is already satisfied.

## TASK

Implement Signal B3 (amount co-occurrence) end-to-end: add the `transactions` table in both SQL dialects plus a migration pair, add one tenant-scoped query helper to `core/graph/entity_store.py`, wire the B3 branch into `core/matching/scoring.py::_compute_b_boosts` and `score_pair`, update the schema-parity lists, and add the enumerated tests.

## FILE PATHS

**Create:**
- `db/migrations/003_transactions.sql`
- `db/migrations/003_transactions_sqlite.sql`

**Modify:**
- `db/schema.sql` — add `transactions`
- `db/schema_sqlite.sql` — add `transactions`
- `core/graph/entity_store.py` — add `AMOUNT_TOLERANCE_PCT`, `AMOUNT_TOLERANCE_CAP`, `count_amount_cooccurrence_periods`
- `core/matching/scoring.py` — `"B3"` in both `Literal`s, B3 block in `_compute_b_boosts`, wiring in `score_pair`
- `tests/test_schema_parity.py` — `SHARED_TABLES` and `EXCLUDE` (both edits)
- `tests/test_scoring.py` — append a new B3 test section

**DO NOT MODIFY:**
- `tests/test_scoring.py:886 test_b_boosts_total_applied_capped_at_max` — must remain byte-identical. The 5-signal cap case is a NEW, separately named test.
- `core/matching/disposition.py` (thresholds at `:54-56` stay 0.90 / 0.70 / 0.50)
- `core/matching/weights.py`
- Any B1, B2, B4, B5, B6 or Signal Set C logic
- `db/views/` (untouched by this feature)
- `TEMPLATE.md`, `.claude/settings.json`, `.claude/hooks/`, `.claude/agents/`
- `db/migrations/002_*` and `db/schema.sql`'s existing four tables

## CONVENTIONS

**Pinned table shape** (dialect types split as `SQLite | Postgres`):

```sql
transactions(
  txn_id                 INTEGER PRIMARY KEY AUTOINCREMENT | BIGSERIAL PRIMARY KEY,
  tenant_id              TEXT | UUID NOT NULL REFERENCES tenants(id),
  source                 TEXT NOT NULL,          -- 'quickbooks' | 'ruddr'
  category               TEXT NOT NULL,          -- 'accounting' | 'psa'
  external_source_id     TEXT NOT NULL,          -- source-system transaction id
  txn_type               TEXT NOT NULL,          -- 'invoice' | 'payment' | 'bill' | 'time_entry' | ...
  amount                 REAL | NUMERIC(18,2) NOT NULL,
  currency               TEXT NOT NULL DEFAULT 'USD',
  txn_date               TEXT | DATE NOT NULL,   -- ISO-8601
  period                 TEXT NOT NULL,          -- 'YYYY-MM', derived from txn_date at insert
  counterparty_source_id TEXT,                   -- source-system entity id the txn belongs to
  canonical_id           TEXT REFERENCES canonical_entities(canonical_id),
  created_at             TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP | TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (tenant_id, source, external_source_id)
);
CREATE INDEX IF NOT EXISTS transactions_counterparty ON transactions (tenant_id, source, counterparty_source_id, period);
CREATE INDEX IF NOT EXISTS transactions_canonical    ON transactions (tenant_id, canonical_id, period);
```

- Mirror the `canonical_entities` `tenant_id` typing pattern per dialect exactly (TEXT in SQLite, UUID FK in Postgres) to avoid parity drift.
- Column name is `external_source_id`, not `source_txn_id` — it parallels `system_references(source, external_id)` / `UNIQUE (tenant_id, source, external_id)` at `db/schema.sql:81`.
- Index names follow the existing `<table>_<suffix>` convention (`canonical_entities_type`, `system_references_canonical`). No `idx_` prefix.
- **Tenant scoping = a `tenant_id` column plus explicit WHERE predicates.** Do NOT write `CREATE POLICY`; no row-level security exists anywhere in this tree and none is to be invented (rules §0, §10).
- **Migrations use `CREATE TABLE IF NOT EXISTS`.** Do NOT copy migration 002's Postgres `DROP TABLE ... CASCADE` pattern.

**Join keys (load-bearing).** On the normal Stage 3 path the source entity is unresolved (`source_canonical_id is None`), so B3 must never key on the source's canonical id:
- Source side joins on `(source, counterparty_source_id) = (entity.source, entity.source_id)`.
- Candidate side joins on the nullable `canonical_id` column.

**Period semantics.** `period` is a `'YYYY-MM'` bucket derived from `txn_date` at insert time — never from insert/load time. The helper must contribute nothing for a row whose `period` disagrees with its `txn_date` month.

**Amount tolerance — symmetric by construction.** A co-occurrence exists in period P when a source-side row and a candidate-side row from **different source systems, same currency** satisfy:

```
ABS(ABS(a.amount) - ABS(b.amount)) <= MIN(MAX(ABS(a.amount), ABS(b.amount)) * 0.02, 500)
```

Anchoring on `MAX(|a|,|b|)` makes `B3(a,b) == B3(b,a)` by construction. `ABS()` on the amounts handles credit memos.

**New constants,** module-level in `core/graph/entity_store.py`, next to the query that applies them:
```python
AMOUNT_TOLERANCE_PCT: float = 0.02
AMOUNT_TOLERANCE_CAP: float = 500.0
```

**New helper** in `core/graph/entity_store.py`, following that module's conventions (module-level function, parameterized SQL only, explicit `if tenant_id is None:` branch with two separate query strings — no SQL string concatenation, `tenant_id` last):
```python
count_amount_cooccurrence_periods(conn, source, source_entity_id, candidate_canonical_id, tenant_id=None) -> int
```
Returns the number of DISTINCT periods containing at least one co-occurring row pair. One query — no per-row Python filtering.

**`_compute_b_boosts` changes:**
- Add `"B3"` to BOTH `Literal` annotations — the `BoostEntry.signal_id` field (`scoring.py:78`) and the local `raws` annotation (`scoring.py:239`). Missing either is a type-level regression.
- New optional parameter `amount_cooccurrence_periods: Optional[int] = None`, appended in the existing style; `None` means "skip B3" so existing direct-call tests keep working unchanged.
- The B3 block sits **inside** the existing band gate at `scoring.py:236`, like every other B signal.
- Tiering (mirrors B2): `+0.10` for exactly one distinct co-occurring period, `+0.15` for two or more. A count of `0` appends **nothing** to `raws`.
- Itemized in `signal_breakdown` with signal id, raw and applied values.

**`score_pair` wiring:** call the helper **at most once per pair and only when `in_b_band`**. Follow the **`candidate_ext` gating pattern** (`scoring.py:689-693`) — a conditional placed after line 688 — NOT the `person_count`/`neighbor_count` pattern at `:679-684`, which is unconditional. Key the entity side on `(entity.source, entity.source_id)`, never on `source_canonical_id`.

**Cap logic is untouched.** B3 adds a raw value to `raws` and nothing else. Do not add a per-signal clamp; do not change the renormalization at `scoring.py:303`.

**Schema parity — TWO mandatory edits** at `tests/test_schema_parity.py:46-51`: append `"transactions"` to `SHARED_TABLES` AND add `"transactions": {"tenant_id"}` to the `EXCLUDE` dict. `test_column_parity` subscripts `EXCLUDE[table]` at `:121-122` with no `.get()` and no default, so a `SHARED_TABLES` entry without its `EXCLUDE` key raises `KeyError`. The value is `{"tenant_id"}`, not `set()` — matching the `canonical_entities` (`:47`) and `system_references` (`:50`) convention; `set()` is only for tables with no `tenant_id` column in either dialect.

**Test data:** there are no transaction fixtures. Tests seed raw SQL rows shaped by the semantics above; ground-truth suites intentionally leave B3 dark. Real-data validation is deferred to the ingestion follow-up.

Python style: snake_case functions, PascalCase classes, UPPER_CASE constants. Log writes (SHIPPED.md, DEBUG.md, RUN_LOG.md, PROMPT_LOG.md, CC-LEARNINGS.md) are append-only. Feature branches only — never commit to main.

## ACCEPTANCE CRITERIA

Every criterion must be asserted by the test command below.

1. `transactions` exists in BOTH `db/schema.sql` and `db/schema_sqlite.sql` with the pinned columns, `UNIQUE (tenant_id, source, external_source_id)`, and the two indexes; `tests/test_schema_parity.py` receives BOTH required edits (`"transactions"` in `SHARED_TABLES` AND `"transactions": {"tenant_id"}` in `EXCLUDE`); `test_column_parity` passes for all five tables.
2. `db/migrations/003_transactions.sql` and `db/migrations/003_transactions_sqlite.sql` both exist and use `CREATE TABLE IF NOT EXISTS`; the SQLite migration applies cleanly and idempotently on top of `db/schema_sqlite.sql`.
3. **(1) Symmetry** — `B3(entity → candidate) == B3(candidate → entity)` at a boundary amount, asserted via two mirrored `score_pair` calls.
4. **(2) Credit memo** — amounts `-1000.00` and `1000.00` co-occur (ABS semantics).
5. **(3) Tolerance boundary** — a delta exactly equal to `MIN(MAX(|a|,|b|)*0.02, 500)` fires; one cent over does not. Covered on both sides of the `MIN` (percentage-bound and $500-bound).
6. **(4) Tiering** — 1 distinct period → raw `+0.10`; 2 distinct periods → raw `+0.15`; a single period containing three qualifying row pairs still counts once.
7. **(5) Cross-currency** — USD and EUR rows with identical amounts never co-occur.
8. **(6) Same-source** — two `quickbooks` rows never co-occur with each other.
9. **(7) Band gate** — B3 produces no entry at `base_score` 0.65 or 0.92 even with perfect co-occurrence.
10. **(8) Cap collision (NEW test)** — B1+B2+B3+B4+B5 with raw sum > 0.20 renormalizes proportionally: `sum(e.applied for e in entries) == 0.20` exactly, each `applied == raw * (0.20 / total_raw)`, and all five entries present in `signal_breakdown`. The shipped 4-signal cap test at `tests/test_scoring.py:886` is untouched.
11. **(9) Period derivation** — a seeded row with `txn_date = '2026-03-15'` carries `period = '2026-03'`; the helper contributes 0 for a row whose `period` disagrees with its `txn_date` month.
12. **(10) Tenant scoping** — rows under a different `tenant_id` are invisible to the helper when `tenant_id` is set.
13. **(11) Dark by default — proven in-process, no baseline artifact** — with an empty `transactions` table: (a) `count_amount_cooccurrence_periods` returns `0`; (b) no `BoostEntry` with `signal_id == "B3"` appears in `signal_breakdown.b_boosts`; (c) the score is proven unchanged **within a single test, computing both sides in-process — no committed baseline fixture, no comparison against any recorded pre-8b number**. For one pair and one `conn`, call `_compute_b_boosts(...)` twice with identical arguments except the new parameter: once with `amount_cooccurrence_periods=None` and once with `amount_cooccurrence_periods=0`. Assert the two returned `tuple[BoostEntry, ...]` are equal element-for-element on `signal_id`, `raw` and `applied`, then assert `_weighted_score(base_score, boosts_disabled) == _weighted_score(base_score, boosts_enabled)` using exact `==`, not `pytest.approx`. The tuple equality is the load-bearing half: because the budget is proportional renormalization (`scoring.py:303`), a spurious zero-valued B3 entry appended to `raws` would raise `total_raw` and rescale every other signal's `applied`. The correct implementation appends nothing to `raws` when the period count is 0.
14. **(12) Unresolved-source path** — B3 fires for a pair whose source entity has `source_canonical_id is None`, proving the join keys do not require resolution.
15. `_compute_b_boosts(..., amount_cooccurrence_periods=None)` skips B3 entirely; all pre-existing direct-call tests pass unmodified.
16. `score_pair` invokes `count_amount_cooccurrence_periods` at most once per pair and only when `in_b_band`.
17. Full suite green with no regression to the 8a baseline, run twice: once as-is, and once with `models/cc.en.300-compress.bin` temporarily moved aside (both must pass; restore the file afterwards).

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Run from the repo root. Bare `pytest` is NOT on PATH, and `.venv/bin/pytest` fails to put the repo root on `sys.path`, so every `from core...` / `from connectors...` import fails to collect — use the `python -m` form exactly as written. Run it before the first change to record a baseline, and after each step.

## NON-GOALS

- **Connector ingestion.** No QB/RUDDR `read_transactions` → persist wiring. Rows enter only via a test helper seeding raw SQL. A follow-up feature owns ingestion. B3 being dark outside tests is an accepted, recorded consequence — not a defect to fix here.
- **Currency conversion.** Same-currency is required; cross-currency rows simply never co-occur. The $500 cap is USD, V1.
- **Amount aggregation.** Row-level co-occurrence only. Do not sum RUDDR entries per project per period.
- **Period windows beyond exact-month equality.** Billing lag will cause false negatives; accepted for V1.
- **Any change to B1, B2, B4, B5, B6**, to Signal Set C, to `core/matching/weights.py`, or to `core/matching/disposition.py`.
- **Any change to shipped 8a tests**, in particular `tests/test_scoring.py:886 test_b_boosts_total_applied_capped_at_max`.
- **Stage 6 graph-update wiring.**
- **No new pip dependencies.**
- **No raising of disposition cutoffs** (0.90 / 0.70 / 0.50 at `core/matching/disposition.py:54-56`).
- **No `CREATE POLICY` / Postgres row-level security.**
- **No per-signal clamp** and no change to the `MAX_B_BOOST` renormalization logic.
- Neo4j, fine-tuned fastText, XGBoost, GraphRAG, self-hosted LLM, write-back all remain NOT-SCOPE. B3 adds a SQL join, no LLM surface.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)