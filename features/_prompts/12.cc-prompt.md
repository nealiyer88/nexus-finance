```markdown
## SITUATION

Read these files before writing any code:

- `CLAUDE.md` (repo root) — test-command form, naming rules, append-only log rule, feature-branch rule.
- `.claude/rules/01-nexus-finance-v1.md` — architectural guardrails. §0 (BUILT vs PLANNED), §5 (confidence thresholds), §6 (6-stage pipeline), §11 (NOT-SCOPE), §12 (owner map), §13 (session guardrails).
- `features/pipeline/matcher-orchestrator.md` — the feature brief this prompt implements (feature 12).
- `core/matching/types.py` — `Disposition`, `Action`, `ScoredMatch`, `SignalBreakdown`, `GraphEvidence`, `LLMAssessment`, `DeterministicMatch`, `CandidateEntity`, `CandidateSet`. **You will add `MatchContext` and `MatchResult` here.**
- `core/matching/deterministic.py` — Stage 1 `deterministic_match`, `ALIAS_EXACT_CEILING`.
- `core/matching/blocking.py` — Stage 2 `generate_candidates` (note the `embedding_index is not None` guard).
- `core/matching/indices.py` — `TokenIndex.build`, `NgramIndex.build`, `EmbeddingIndex.build`, and the `EmbeddingIndex` docstring on the absent-model-file / pass-`None` contract.
- `core/matching/scoring.py` — Stage 3 `score_candidate_set`; `BoostEntry` lives here, not in `types.py`.
- `core/matching/disposition.py` — Stage 4 `apply_thresholds`, `AUTO_APPROVE_THRESHOLD`, `SURFACE_THRESHOLD`, `LLM_FALLBACK_THRESHOLD`.
- `core/matching/llm_fallback.py` — Stage 5 `llm_assess`, the `LLMClient` `Protocol`, `LLMNotConfiguredError`, `LLMBudgetExceededError`, `reset_call_budget`, `MAX_LLM_CALLS_PER_RUN`, `_default_llm_client_factory`.
- `core/graph/resolution.py` — **Stage 6 lives here, not in `core/matching/`**: `resolve_match`, `create_new_entity`, `reject_match`, plus the staleness helpers `mark_indices_stale()` and `reset_indices_stale_flag()`.
- `core/graph/entity_store.py` — `lookup_alias_exact` docstring (canonical-name-as-seed-alias), `create_canonical_entity` (id minting, `entity_category` column), `_assert_tenant_scope`.
- `core/matching/pending_store.py` — feature 10b's `enqueue_pending`. **Read its signature with `inspect.signature`; do not transcribe parameter names from prose.**
- `core/matching/training_data.py` — the self-gating training-pair store the Stage 6 writers already call.
- `core/ingestion/normalizer.py` — `NormalizedEntity` and its two distinct category fields.
- `connectors/base.py` — `ConnectorInterface` ABC.
- `connectors/quickbooks.py`, `connectors/ruddr.py` — `category` class attrs, `read_entities`, and the entity-type maps `_QB_TYPE_FOR_ENTITY` / `_RUDDR_TYPE_FOR_ENTITY`.
- `db/schema_sqlite.sql` and the contents of `db/migrations/` (list the directory).
- `tests/test_pending_decisions.py`, `tests/test_resolution.py`, `tests/test_llm_fallback.py` — the `REPO_ROOT` / `SQLITE_SCHEMA` / migration path-constant + `executescript` test pattern to copy.

Skills: None.

Current state (verified against HEAD by a pre-build reality check — these facts win over any prose):

- `core/matching/engine.py` and `core/ingestion/pipeline.py` do NOT exist. `core/matching/` holds blocking.py, deterministic.py, disposition.py, embeddings.py, indices.py, llm_fallback.py, pending_store.py, redaction.py, scoring.py, training_data.py, types.py, weights.py. `core/ingestion/` holds only `__init__.py` and `normalizer.py`.
- `MatchContext` and `MatchResult` are absent from `core/matching/types.py`. **No `EntityRegistry` type exists anywhere in the tree** — do not create one; `MatchContext` replaces it.
- Shipped stage signatures (read them again at build time; these are what the reality check observed):
  - `deterministic_match(entity, conn, tenant_id=None) -> Optional[DeterministicMatch]` — `core/matching/deterministic.py:40`
  - `generate_candidates(entity, token_index, ngram_index, conn, tenant_id=None, embedding_index=None) -> CandidateSet` — `core/matching/blocking.py:46`
  - `score_candidate_set(entity, candidate_set, conn, tenant_id=None, source_canonical_id=None) -> tuple[ScoredMatch, ...]` — `core/matching/scoring.py:749`
  - `apply_thresholds(source_entity_id: str, candidates, conn, tenant_id=None) -> Disposition` — `core/matching/disposition.py:91`
  - `llm_assess(disposition, entity, conn, tenant_id=None, client=None) -> Disposition` — `core/matching/llm_fallback.py:468`; returns a NEW `Disposition` already set to `action="QUEUE_FOR_REVIEW"`.
  - `enqueue_pending(conn, disposition, entity, proposal, tenant_id=None) -> Optional[str]` — `core/matching/pending_store.py:337`.
- `TokenIndex.build` / `NgramIndex.build` / `EmbeddingIndex.build` all take `(conn, tenant_id=None)` and rebuild from the connection alone.
- Stage 6 has **no** `resolve(disposition)` function. It ships three decision-specific writers: `resolve_match` (resolution.py:141), `create_new_entity` (:215), `reject_match` (:303). All three expose `reasoning_trace: str = ""`. `resolve_match` and `create_new_entity` require `approved_by`; `reject_match` does not take one.
- The module-level staleness dirty bit is real: `mark_indices_stale() -> bool` (resolution.py:57) and `reset_indices_stale_flag()` (:75). The internal `_flag_indices_stale()` fires only after a successful graph write in `resolve_match` / `create_new_entity`; `reject_match` never sets it.
- The Stage 6 writers each own their transaction (`conn.commit()` / `conn.rollback()` inside). `pending_store.py` never commits or rolls back — the caller owns that boundary.
- `store_training_pair` is already called on all three Stage 6 paths and self-gates on an LLM `call_id`. **No new persistence code is needed for training pairs.**
- `enqueue_pending` self-gates: `_ENQUEUABLE_ACTION = "QUEUE_FOR_REVIEW"` (:56) and `if disposition.action != _ENQUEUABLE_ACTION: return None` (:369). It returns `Optional[str]` (a `pending_id`) — do not branch on the return value.
- **Stage 5 failure path caveat:** when `llm_assess` raises, you still hold the ORIGINAL disposition whose `action` is `"LLM_FALLBACK"`, and `enqueue_pending` would write nothing. `Disposition` is `frozen=True`, so build the queued disposition with `dataclasses.replace(...)` before calling enqueue. This is the disposition conversion the brief mandates ("record the entity as queued for review with no assessment") — it is NOT an action check around the call, which remains forbidden.
- `pending_decisions` is NOT in `db/schema_sqlite.sql` (whose CREATE TABLEs are `canonical_entities`, `entity_aliases`, `entity_edges`, `system_references`, `transactions`). It arrives via a SQLite migration under `db/migrations/`. `transactions` needs no migration.
- `tests/test_pending_decisions.py:46-50` already demonstrates the path-constant + glob pattern for locating migration files at test time.
- `Action = Literal["AUTO_APPROVE", "QUEUE_FOR_REVIEW", "LLM_FALLBACK", "NO_MATCH"]` (`types.py:116`) — the exact four-branch dispatch space.
- **Connectors already return `NormalizedEntity`** — `read_entities` normalizes internally. `run_ingestion` must NOT re-normalize.
- **The connector entity-type maps contain aliases.** `_QB_TYPE_FOR_ENTITY` (quickbooks.py:138) maps both `customer` and `client` → `Customer`, and both `person` and `employee` → `Employee`; `_RUDDR_TYPE_FOR_ENTITY` (ruddr.py:117) maps `person`, `team-member` and `resource` → `team-member`. Use one canonical type per source (QB: customer/vendor/person; RUDDR: client/vendor/person) or the same records are pulled twice, `(source, source_id)` collides in `_compute_decision_key`, and the pending-row count silently undercounts.
- `create_new_entity` writes **no** `entity_aliases` row. Later same-run deterministic hits rely entirely on `lookup_alias_exact` treating `canonical_entities.canonical_name` as a seed alias at confidence 1.0 — so the `canonical_name` argument you pass must be the **normalized** name.
- `create_new_entity` mints ids as `f"{entity_type.upper()}_{hex8}"` (entity_store.py:695): the **canonical** entity type, not the connector's source type, determines the id prefix. Establish the source-type → canonical-type mapping explicitly in `pipeline.py`.
- `NormalizedEntity` has two distinct category fields: `entity_category` (`organization` / `person` — the axis Stage 1 and Stage 5 branch on, and the axis `create_new_entity`'s `entity_category` argument feeds) and `category` (`accounting` / `psa` — the axis Stage 3's weight dispatch uses). Confirm which axis each writer argument wants against the shipped readers.
- Fixtures contain cross-connector exact normalized-name collisions (organizations and persons alike), so same-run deterministic hits at `ALIAS_EXACT_CEILING = 0.99` ≥ `AUTO_APPROVE_THRESHOLD = 0.90` are guaranteed on a first run over an empty graph. Do not assert zero auto-approvals on run one.
- `(source, source_id)` is unique across all fixture entities from both connectors.
- `reset_call_budget()` (llm_fallback.py:162) and `reset_indices_stale_flag()` (resolution.py:75) both exist as per-run reset hooks.
- `LLMClient` is a `Protocol`; the default client is constructed only when `client is None`, so an injected fake fully avoids it.
- There is NO audit table in the live SQLite store and no runtime audit writer. Feature 10a owns that path and is BLOCKED.
- Baseline collection observed by the reality check: 400 tests collected, clean. Re-measure at build time; do not trust the number.

## TASK

Build the end-to-end matcher orchestrator: `match()` in `core/matching/engine.py`, `run_ingestion()` in `core/ingestion/pipeline.py`, the `MatchContext` and `MatchResult` types in `core/matching/types.py`, and the two test modules `tests/test_engine.py` and `tests/test_pipeline.py`.

## FILE PATHS

Create:
- `core/matching/engine.py`
- `core/ingestion/pipeline.py`
- `tests/test_engine.py`
- `tests/test_pipeline.py`

Modify:
- `core/matching/types.py` — additive only: add `MatchContext` and `MatchResult`. Do not change any existing declared type.

DO NOT MODIFY:
- `core/matching/deterministic.py`, `blocking.py`, `indices.py`, `scoring.py`, `disposition.py`, `llm_fallback.py`, `pending_store.py`, `training_data.py`, `redaction.py`, `weights.py`, `embeddings.py`
- `core/graph/resolution.py`, `core/graph/entity_store.py`
- `core/ingestion/normalizer.py`
- `connectors/base.py`, `connectors/quickbooks.py`, `connectors/ruddr.py`
- `db/schema.sql`, `db/schema_sqlite.sql`, anything under `db/migrations/`
- `tests/test_schema_parity.py` and every other existing file under `tests/`
- `requirements.txt`
- `features/TEMPLATE.md`, `.claude/settings.json`, anything under `.claude/hooks/` or `.claude/agents/`

## CONVENTIONS

- Python 3.10+ via `.venv`. `snake_case` functions, `PascalCase` classes, `UPPER_CASE` constants.
- Signature convention across `core/`: `conn: sqlite3.Connection` early, `tenant_id: Optional[str] = None` last. Thread `tenant_id` into every stage call and into `enqueue_pending`, sourced from `MatchContext` / `run_ingestion`'s tenant argument.
- `MatchContext` is a **mutable** dataclass (NOT frozen) — its index fields are replaced in place by the rebuild policy. Its field list is exactly the union of what the shipped stage signatures demand: at minimum the open connection, the optional `tenant_id`, the token index, the n-gram index, the optional embedding index, and the optional LLM client injected into Stage 5. **Derive the field list by reading the live signatures at build time**, not from this prompt's prose.
- `embedding_index` may legitimately be `None` (absent fastText model file). The orchestrator must tolerate `None` and a built index alike, and pass it through to Stage 2 unchanged.
- `MatchResult` carries ONLY these fields, each of which has a shipped producer:
  - `source_entity_id` — from the incoming `NormalizedEntity`
  - `canonical_id: Optional[str]` — the deterministic hit, the resolved canonical, or the id returned by the new-entity writer
  - `confidence: float` — the deterministic match confidence, or the top scored match's score
  - `match_type` — a `Literal` over the paths the orchestrator can actually take: deterministic / scored / llm / new / none. **There is no `human` value** — the orchestrator never has a human in the loop.
  - `action` — the `Disposition.action` value that decided the outcome
  - `signal_breakdown: Optional[SignalBreakdown]` — from `ScoredMatch.signal_breakdown`; `None` on the deterministic and empty-candidate paths, which never reach scoring
  - `disposition: Optional[Disposition]` — the Stage 4/5 result when one was produced
  - `reasoning_trace: str`
  - **No `audit_entry` field.** It has no producer, no table and no writer anywhere in the tree.
- **Auto-approval identity:** define `AUTO_APPROVAL_ACTOR = "system:matcher-orchestrator"` as an `UPPER_CASE` module constant in `core/matching/engine.py`. Every other caller imports it. Never an inline string literal.
- **Stage 6 dispatch is a four-branch table over the `Action` value space** (derive the value set with `typing.get_args` on the `Action` alias — do not hardcode it): the three resolution writers plus `QUEUE_FOR_REVIEW`. Supply each writer the arguments its live signature requires, read with `inspect.signature` at build time. `AUTO_APPROVAL_ACTOR` is the approver on the auto path; the reasoning trace goes in via the `reasoning_trace` parameter the writers already expose.
- **The `QUEUE_FOR_REVIEW` branch is not a no-op.** Do the same write-argument derivation the resolution branch would have done, then hand that JSON-serializable keyword-argument mapping to `enqueue_pending` as the proposal instead of to the writer. Derive the enqueue parameter names from its live signature. Add **no action check of your own** around that call — it self-gates, exactly as the training-pair store does.
- **Training-pair persistence is a pass-through with zero new code.** Pass the reasoning trace into the Stage 6 writer and let the writer's existing `store_training_pair` call happen. Do not suppress it and do not add a second persistence path.
- **Index rebuild policy (a stated requirement, not a builder choice):** before Stage 2, for every incoming entity, check the Stage 6 staleness bit; when it is set, rebuild all three indices from the connection and reset the flag. There is no incremental update path. The bit is only set by writers that actually mutated the graph, so entities producing no write skip the rebuild.
- **Stage 5 failure handling:** catch both `LLMNotConfiguredError` and `LLMBudgetExceededError`, record the entity as queued for review with no assessment (via `dataclasses.replace` to a `QUEUE_FOR_REVIEW` disposition), persist it through the same enqueue call, and continue the batch. A batch run must never abort because the LLM path was unavailable or exhausted.
- `run_ingestion(connector: ConnectorInterface, tenant_id: str)` pulls entities from the connector for each supported entity type (one canonical type per source — see the alias trap above), handles both org and person entity types, matches each via `match()`, resets the Stage 5 call budget at the start of each run, and processes **sequentially** — no async, no parallel, no queue.
- The run summary's buckets are **mutually exclusive and exhaustive** over the entities ingested, keyed off the final `MatchResult`, and the summary also reports the ingested total. Every entity lands in exactly one bucket.
- **Derive, do not transcribe.** Stage signatures and the enqueue signature come from `inspect.signature`; the `Action` value set from `typing.get_args`; migration filenames from listing `db/migrations/`; the end-to-end entity total from counting the connectors' output at test time. No parameter lists, counts, or filenames hardcoded from this prompt.
- **Tests build their schema from files only.** `executescript` `db/schema_sqlite.sql`, then the SQLite-dialect LLM-training-data migration, then the SQLite-dialect pending-decisions migration — both located by listing/globbing `db/migrations/` at test time, following the `REPO_ROOT` path-constant pattern in `tests/test_pending_decisions.py`. Never `CREATE TABLE` inside a test. Without the pending-decisions table, the queued branch raises a missing-table error at runtime.
- Tests must reset `reset_indices_stale_flag()` between runs and `reset_call_budget()` at the start of each run.
- No `psycopg`, no `DATABASE_URL`, no Postgres, no `approval_decisions`, no `audit_log` in any new file.
- Feature branches only — never commit to `main`. All log writes are append-only.

## ACCEPTANCE CRITERIA

1. `core/matching/engine.py` exists with a `match()` function and the `AUTO_APPROVAL_ACTOR` constant.
2. `core/ingestion/pipeline.py` exists with `run_ingestion()`.
3. `MatchContext` and `MatchResult` are defined in `core/matching/types.py` and `MatchResult` has no `audit_entry` field.
4. End-to-end test: every fixture entity returned by both shipped connectors is processed through the full pipeline without errors, where the expected total is obtained by counting the connectors' output at test time.
5. **Bucket accounting invariant:** the run summary's buckets are pairwise disjoint, every processed entity appears in exactly one, and the bucket counts sum to the ingested total. No per-bucket count is written into the test.
6. **First-run invariant, NOT a per-bucket number.** Assert only the invariant — every entity is accounted for, and no bucket count is asserted against a literal. (Do not assert zero auto-approvals on an empty graph: Stage 1's exact-alias lookup queries SQLite directly, never consults the in-memory blocking indices, and treats a canonical name as a seed alias at full confidence; the shipped fixtures contain exact normalized-name collisions, so later entities with a colliding normalized name match deterministically above the auto-approve threshold on the *same* run.)
7. **Second-run criterion as a derived proportion:** capture the first run's summary, simulate approvals, re-run, and assert the auto-approved share of the second run is strictly greater than the first run's — comparing the two runs' own numbers. No target number is written into the test.
8. **Pending-persistence invariant:** after a run, the number of rows persisted to the pending-decisions table equals the run summary's queued bucket count — both read at test time from the run's own outputs, with no literal count written into the test. The test's connection must have the pending-decisions migration applied, or this criterion fails with a missing-table error rather than a count mismatch.
9. Match type distribution tracked across the paths the orchestrator can emit.
10. Pipeline summary reports entity counts per bucket plus the ingested total.
11. The repo's documented test invocation, scoped to the new test modules, passes — and the assertion is a **collected count greater than zero** parsed from the collection output, not a bare exit code (a deselected-everything run exits 0). Concretely: `.venv/bin/python -m pytest tests/test_engine.py tests/test_pipeline.py --collect-only -q` reports a collected count > 0, and `.venv/bin/python -m pytest tests/test_engine.py tests/test_pipeline.py -x --tb=short` passes.
12. Index rebuild policy is implemented as stated: the staleness bit is checked per entity before blocking, and a set bit triggers a full three-index rebuild and a flag reset.
13. No test reads an API key or constructs the default LLM client. Stage 5 is exercised with an injected fake `LLMClient` and a reset call budget; the fake is passed on every Stage 5 invocation, the test asserts against the fake's own recorded call count, and **no test makes a live API call**.

Additionally, the full suite must pass with no regression: `.venv/bin/python -m pytest tests/ -x --tb=short` green, with a collected count **greater than or equal to** the count collected on the commit before this feature (measure the baseline at build time; the reality check observed 400 — re-measure, do not trust the number).

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Bare `pytest` is NOT on PATH, and `.venv/bin/pytest` fails to put the repo root on `sys.path` — every `from core...` / `from connectors...` import then fails to collect. Always use the `python -m` form, from the repo root.

## NON-GOALS

- Celery/Redis or any async queue — V1 runs synchronously.
- Webhook-triggered ingestion — V1 uses a manual/scheduled trigger.
- Multi-connector orchestration (running QB + RUDDR in one call) — a later scheduling/worker layer owns that; `run_ingestion` takes one connector per call.
- Dashboard integration.
- **A persisted audit table or audit writer.** There is no audit table in the live SQLite store and no runtime audit writer; the audit DDL is Postgres-only and its middleware is a stub. Feature 10a owns that path and is BLOCKED. Do not create one, and do not add an `audit_entry` field.
- **The human review queue SURFACE — feature 11.** No API route, no Dash page, no badge count, no list/claim/decide flow. **This exclusion covers the surface only and does not cover the persistence call:** writing the pending row via feature 10b's shipped enqueue function is explicitly IN scope. Skipping the enqueue call because "the review queue is out of scope" is a misread and fails criterion 8.
- Any change to feature 10b's `core/matching/pending_store.py`, to the training-pair writer, or to any shipped stage module — this feature calls them, it does not edit them.
- Postgres work of any kind: no `requirements.txt` entry, no driver import, no new environment variable.
- An `EntityRegistry` type — `MatchContext` replaces it; nothing in the tree takes one.
- A `resolve(disposition)` convenience wrapper over the three Stage 6 writers.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)
```