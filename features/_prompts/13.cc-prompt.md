## SITUATION

Read before writing any code:
- `CLAUDE.md` (repo root) — test invocation, naming, append-only log rules, branch rules
- `.claude/rules/01-nexus-finance-v1.md` — BUILT vs PLANNED markers, thresholds (§5), 6-stage pipeline (§6), NOT-SCOPE (§11), owner map (§12)
- `core/matching/engine.py` — `match(incoming: NormalizedEntity, ctx: MatchContext) -> MatchResult` (defined at `engine.py:215`)
- `core/matching/types.py` — `ScoredMatch` (`:98-112`), `Disposition` (`:156-185`), `MatchContext` (`:196-218`), `MatchResult` (`:221-253`)
- `core/ingestion/pipeline.py` — `run_ingestion(connector, conn, tenant_id, llm_client=None) -> IngestionSummary` (`:81-86`), `_ENTITY_TYPES_BY_CATEGORY` (`:34-37`)
- `core/matching/disposition.py` — `apply_thresholds`, `AUTO_APPROVE_THRESHOLD`, `SURFACE_THRESHOLD`, `LLM_FALLBACK_THRESHOLD` (`:54-56`)
- `core/matching/llm_fallback.py` — `llm_assess(disposition, entity, conn, tenant_id=None, client=None)` (`:468-473`), `MAX_LLM_CALLS_PER_RUN` (`:79`), `LLMBudgetExceededError`
- `core/matching/redaction.py` — `redact_org`, `redact_person`, `leak_check`
- `core/matching/pending_store.py` — `enqueue_pending` (`:464`), `list_pending` (`:572`), `mark_decided` (`:641`), `PendingDecision` (`:210`)
- `connectors/base.py` — `ConnectorInterface`, `NormalizedEntity` (`.category` is `"accounting" | "psa"`, `:77`), `DateRange` (`:60`)
- `tests/test_pipeline.py` — the existing shape for a fixture-backed seeding run and the simulated-approval + re-run pattern (`:167-198`, `:327`)

Skills: None.

Current state (verified against the tree):
- Features 11 and 12 are SHIPPED. `core/matching/engine.py`, `core/ingestion/pipeline.py`, `core/matching/pending_store.py`, `db/migrations/004_pending_decisions_sqlite.sql`, `api/routers/approvals.py` all exist.
- Neither target file exists: `core/ingestion/historical.py`, `core/ingestion/clustering.py`, `tests/test_historical.py` are all absent, and no `seed_from_history` or `cluster_entities` symbol exists anywhere in the tree. No success criterion is already satisfied.
- `MatchResult.disposition` and `MatchResult.signal_breakdown` are BOTH `None` on the Stage 1 deterministic short-circuit (`engine.py:242-253`) and on the no-candidates path (`engine.py:280-289`). On an empty graph the entire first connector run takes the no-candidates path.
- `IngestionSummary` exposes `results: tuple[MatchResult, ...]` — that tuple is the intended input to `cluster_entities`.
- `Disposition.candidates_ranked` is already deduped by `canonical_id` and sorted descending by `(score, ascending canonical_id)` (`types.py:165-166`) — rely on that ordering rather than re-sorting.
- `Disposition.abbreviation_rescue` exists as a field (`types.py:185`) — preserve it on member pairs; do not re-derive it.
- `run_ingestion` calls `reset_call_budget()` at the top of every run (`pipeline.py:95`). A two-connector seed therefore resets the Stage 5 budget twice, once per `run_ingestion` call. Do NOT add a third reset.
- `match()` already catches `(LLMNotConfiguredError, LLMBudgetExceededError)` and rewrites the disposition to queue-for-review (`engine.py:311`). Going through `match()` (via `run_ingestion`) inherits that degrade-don't-fail behavior.
- `llm_assess` refuses any disposition whose action is not `LLM_FALLBACK` (`llm_fallback.py:484-486`) and always returns `action="QUEUE_FOR_REVIEW"` (`:646`).
- `enqueue_pending` is a no-op unless the action is `QUEUE_FOR_REVIEW` and a top match exists (`pending_store.py:59`, `:473`, `:485-488`).
- `approval_decisions` is NOT in `db/schema_sqlite.sql` (which creates only `canonical_entities`, `entity_aliases`, `entity_edges`, `system_references`, `transactions`). It is Postgres-only and must be neither created nor queried at runtime.
- Connectors return `NormalizedEntity` directly (`pipeline.py:90-92`, `connectors/quickbooks.py:247-267`). There is NO Stage 0 `normalize_entity()` call in this feature.
- `ConnectorInterface.read_entities(entity_type, filters)` takes no date range (`connectors/base.py:207`); `read_transactions(date_range)` takes a `DateRange` (`:218`). There is no "pull everything" method.
- Fixtures exist: `tests/fixtures/qb_entities.json`, `tests/fixtures/ruddr_entities.json`. Their entity types map fully onto the pipeline's per-category entity-type lists, so no fixture record is silently dropped. Derive counts at test time by loading the files — never hard-code them.

## TASK

Build cold-start historical seeding and cross-category clustering: create `core/ingestion/historical.py` and `core/ingestion/clustering.py` on top of the shipped feature 11/12 pipeline, plus `tests/test_historical.py`, and get the test command green.

## FILE PATHS

Create:
- `core/ingestion/historical.py` — `seed_from_history(qb_connector, ruddr_connector, conn, tenant_id, llm_client=None)`
- `core/ingestion/clustering.py` — `cluster_entities(...)`
- `tests/test_historical.py`

Modify:
- None expected. If a genuine integration seam requires it, prefer additive changes and never alter a shipped threshold constant.

DO NOT MODIFY:
- `core/matching/disposition.py` (thresholds are out of scope — no re-banding for cold start)
- `core/matching/redaction.py`, `core/matching/llm_fallback.py`, `core/matching/engine.py`, `core/ingestion/pipeline.py`, `core/matching/pending_store.py`
- `db/schema_sqlite.sql`, `db/schema.sql`, anything under `db/migrations/` (no cluster-level table, no schema change)
- `TEMPLATE.md`, `.claude/settings.json`, anything under `.claude/hooks/` or `.claude/agents/`

## CONVENTIONS

- Python 3.10+ via the project venv. `snake_case` functions, `PascalCase` classes, `UPPER_CASE` constants (per `CLAUDE.md`).
- Imports are repo-root-relative: `from core.matching.engine import match`, `from core.ingestion.pipeline import run_ingestion`, etc. — matching how existing `tests/` and `core/` modules import.
- Reuse the shipped types; do not invent new pair types. Cluster members are built from `core.matching.types.ScoredMatch` (or the `Disposition` that wraps a ranked tuple of them).
- Import threshold constants from `core.matching.disposition`; never restate their numeric values in code or tests.
- Sequential processing only — no async, no parallelism (V1 hard constraint).
- SQLite is the only runtime engine; every path uses `sqlite3`.
- Log files (`SHIPPED.md`, `DEBUG.md`, `RUN_LOG.md`, `PROMPT_LOG.md`, `CC-LEARNINGS.md`) are append-only.
- Feature branches only — never commit to `main`.

Behavioral contracts this code must honor:
- `seed_from_history` calls `run_ingestion` once per connector against the same `conn` — one connector per call — pulling entity types via the pipeline's existing per-category mapping. It must not request both `"customer"` and `"client"` from a single connector, and must not re-list entity types.
- `cluster_entities` must accept a `MatchResult` with a missing `disposition` and a missing `signal_breakdown` without raising.
- Clusters derive ONLY from a disposition's ranked candidates. An entity whose disposition is absent (Stage 1 hit, or no candidates) belongs to ZERO clusters — that is expected output, not an error.
- Only members whose disposition action is queue-for-review get an enqueued pending row, written through the shipped pending-decision store. A cluster itself is an in-memory return value — no cluster-level storage.
- LLM-assisted clustering goes through `llm_assess` with the injectable `client` argument. No second redaction implementation may appear in the new files.
- Abbreviation-rescue pairs skip the LLM by design and route straight to human review; preserve `Disposition.abbreviation_rescue` on member pairs rather than re-deriving it.
- Guided-onboarding output: clusters ranked by aggregate confidence, then by cluster size.
- Historical data populates the graph layer only — never modifies source system history.

## ACCEPTANCE CRITERIA

1. `core/ingestion/historical.py` exists with `seed_from_history()`.
2. `core/ingestion/clustering.py` exists with `cluster_entities()`.
3. Historical seeding processes every entity loaded from the fixture files (count derived at test time by loading `tests/fixtures/qb_entities.json` and `tests/fixtures/ruddr_entities.json`) with no unhandled exceptions — asserted at this feature's own module level through `seed_from_history`, not inherited from the existing pipeline tests.
4. Cluster count > 0 and ≤ seeded entity count; no entity appears in two clusters. The `> 0` lower bound is a required non-emptiness precondition, and entities with no pair legitimately appear in zero clusters.
5. Each cluster carries: proposed canonical name, aliases per source category (`accounting` | `psa`, per `NormalizedEntity.category`), aggregate confidence, recommended action.
6. LLM-assisted clustering goes through `llm_assess` / `redaction.py`; no second redaction implementation exists in the new files.
7. No LLM-derived cluster carries an auto-approve recommendation.
8. A second pass after simulated approval of the seeded clusters produces strictly more auto-approvals than the first pass on the empty graph (monotonic improvement, measured in-test against the first pass — not against a fixed percentage), asserted at this feature's own module level through `seed_from_history`, not inherited from the existing pipeline tests.
9. `.venv/bin/python -m pytest tests/test_historical.py -x --tb=short` passes AND reports a collected test count greater than zero (a bare exit code is not acceptable evidence — capture and show the collection line).

Additional assertions required in `tests/test_historical.py` (from the brief's test-suite scope):
- Every pair whose score ≥ `disposition.AUTO_APPROVE_THRESHOLD` lands in the same cluster as its counterpart — import the constant, do not restate its value.
- LLM-assisted clustering is invoked for pairs in the fallback band, using an injected fake `LLMClient` passed via `llm_assess`'s `client` argument. No live API call in tests.

Note on the second pass (repo fact, not a scope change): on the post-seed pass nearly every entity takes the deterministic Stage 1 path, so `disposition` is `None` and that pass legitimately produces zero clusters. Only the first pass is expected to cluster; criterion 8 measures auto-approvals, not clusters.

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Run from the repo root. Bare `pytest` is not on PATH, and `.venv/bin/pytest` fails to put the repo root on `sys.path` — use the `python -m` form. For criterion 9 also run the per-file form and capture its collection line:

```
.venv/bin/python -m pytest tests/test_historical.py -x --tb=short
```

The full suite must be green, not just the new file.

## NON-GOALS

- Retroactive transaction classification (Phase 3 of the historical pipeline).
- Onboarding wizard UI.
- Incremental sync / delta detection — V1 uses full re-pull.
- Cross-category schema drift detection.
- Transaction-volume prioritization.
- Changing any shipped threshold constant, or adding a relaxed/widened cold-start confidence band. Cold start runs on the steady-state thresholds.
- Any cluster-level table or schema change; `approval_decisions` stays Postgres-only, neither created nor queried at runtime.
- Raising `MAX_LLM_CALLS_PER_RUN` or building a new budget mechanism — a cold start that exhausts the cap sends the remainder to human review via the shipped `match()` degrade path.
- Reimplementing matching, ingestion, normalization, or redaction inside the new modules.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)