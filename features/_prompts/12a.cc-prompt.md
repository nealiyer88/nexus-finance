## SITUATION

**Read before writing any code:**
- `CLAUDE.md` (repo root) — test-command and log-append rules
- `.claude/rules/01-nexus-finance-v1.md` — §0 BUILT vs PLANNED, §5 confidence thresholds, §6 pipeline stages, §11 NOT-SCOPE
- `connectors/base.py` — `ConnectorInterface`, `DateRange`, `NormalizedTransaction`
- `connectors/quickbooks.py` — `QuickBooksConnector.__init__`, `_fetch_raw_transactions`, `_map_transaction`, the private fixture loader, `ConnectorError`
- `connectors/ruddr.py` — `RuddrConnector.__init__`, `_fetch_raw_time_entries`, `_map_time_entry`, the private fixture loader
- `db/schema_sqlite.sql` and `db/migrations/003_transactions_sqlite.sql` — the `transactions` DDL (read it; do not edit it)
- `core/graph/entity_store.py` — `count_amount_cooccurrence_periods`, `AMOUNT_TOLERANCE_PCT`, `AMOUNT_TOLERANCE_CAP`, `add_system_reference` (the SELECT-then-branch idiom), `_assert_tenant_scope`
- `core/ingestion/pipeline.py` — `run_ingestion`, `IngestionSummary` (the shape and bucket discipline to mirror)
- `core/matching/pending_store.py` — `get_connection`, `_sqlite_ddl_paths`
- `core/matching/scoring.py` — `score_pair`, `BoostEntry`, the B-boost band constants (read only; do not edit)
- `tests/test_fixture_loads.py` — the `REPO_ROOT / "db" / ...` path-constant + `executescript` test pattern
- `tests/fixtures/generate_fixtures.py` — `validate_fixtures()` returning `(errors, warnings)` and its existing cross-file containment checks
- `tests/fixtures/qb_entities.json`, `tests/fixtures/ruddr_entities.json`, `tests/fixtures/canonical_ground_truth.json` — fixture style and the ids to cross-link against
- `tests/test_scoring.py` — read only, to understand the existing `_insert_txn` seeder. Do not modify or import it.

**Skills:** None.

**Current state (verified against the tree):**
- `transactions` is shipped. DDL is byte-identical in `db/schema_sqlite.sql` and `db/migrations/003_transactions_sqlite.sql`: `tenant_id TEXT` **nullable**, `counterparty_source_id TEXT` nullable, `canonical_id TEXT` nullable FK, `UNIQUE (tenant_id, source, external_source_id)`, indexes `transactions_counterparty` / `transactions_canonical`.
- `NormalizedTransaction` fields: `source_id, source, category, txn_type, amount, currency, txn_date, counterparty_source_id, counterparty_kind, project_code, raw_record`. `DateRange` is inclusive ISO `start`/`end`.
- **Nothing writes a transaction row.** `grep -rn "INSERT INTO transactions" --include=*.py` matches only the private `_insert_txn` seeder in `tests/test_scoring.py`. `tests/fixtures/` has entity fixtures only — no transaction fixture exists.
- Both transaction reads open with the same guard pair in this order: (1) `if self.fixture_path is not None: return []`; (2) `if self.http_client is None: return []`. The live path below is correct and untouched. This makes `_map_transaction` and `_map_time_entry` unreachable in every test.
- `_map_transaction` reads: `Id`/`id`, `_txn_type` or `type` (`"Invoice"`/`"Payment"`/`"Bill"`, lowercased by the mapper), `TotalAmt`/`total_amt`, `CurrencyRef.value`/`currency`, `TxnDate`/`txn_date`, `CustomerRef.value`/`customer_ref` (invoice+payment → `counterparty_kind="customer"`), `VendorRef.value`/`vendor_ref` (bill → `"vendor"`).
- `_map_time_entry` reads: `id`, `hours`, `billing_rate`/`hourly_rate`, `currency`, `date`, `resource_id` → `counterparty_source_id` with `counterparty_kind="resource"`, `project_code` or `project.code`.
- **There is no client-id key in `ruddr_entities.json` today** (`grep -c resource_id` → 0). You choose the RUDDR client-id key name for the new transactions fixture; the counterparty helper must read the same key.
- `count_amount_cooccurrence_periods(conn, source, source_entity_id, candidate_canonical_id, tenant_id=None)` self-joins `transactions` on equal `period`, equal `currency`, differing `source`; source side filtered on `a.source` + `a.counterparty_source_id`, candidate side on `b.canonical_id`; tolerance from `AMOUNT_TOLERANCE_PCT` / `AMOUNT_TOLERANCE_CAP` in `core/graph/entity_store.py`.
- **Tenant symmetry in the B3 join:** with `tenant_id=None` the query applies no tenant predicate at all; with a tenant it adds `b.tenant_id = a.tenant_id` **and** `a.tenant_id = ?`. Both connectors' rows must be ingested under the **same** tenant for a tenant-scoped call to find co-occurrence.
- **B3 is gated by the boost band, and the brief does not say so.** `score_pair` computes `amount_cooccurrence_periods` only when `B_BOOST_BAND_LOW (0.70) <= base_score < B_BOOST_BAND_HIGH (0.90)`; outside the band it passes `None` and `_compute_b_boosts` skips B3 entirely. The B3-`BoostEntry` half of the headline criterion therefore requires the demo pair's **base string score** to land in `[0.70, 0.90)` — choose the fixture pair accordingly. If no B3 boost appears, pick a different fixture pair; **never** edit `core/matching/scoring.py`.
- **`score_pair` keys B3's source side on `(entity.source, entity.source_id)`** — the entity's own source id, not a canonical id. So a QB transaction row's written `counterparty_source_id` must equal the QB entity fixture `id`, and the RUDDR row's `canonical_id` must be the candidate canonical.
- `system_references` has `UNIQUE (source, external_id)` and **no `tenant_id` column** — tenant scoping is done by joining back to `canonical_entities` (shipped convention, see `_assert_tenant_scope`).
- `grep -rn "INSERT OR REPLACE" core/` returns nothing — the ban is already repo-wide. No shipped store module calls `.commit(`/`.rollback(`.
- `pending_store.get_connection` resolves `NEXUS_STORE_PATH` → `DEFAULT_STORE_PATH` and provisions from `[base schema, *sorted(db/migrations/*_sqlite.sql)]`, so a store from that provider already has `transactions`. Add no bootstrap.
- Both connector `__init__`s order params `..., fixture_path, base_url, ...` — the brief says "appended after `fixture_path`"; every construction site in the tree is keyword-only, so inserting the new param after `fixture_path` (before `base_url`) or at the very end are both safe.
- `_fetch_raw_entities`'s fixture branch already filters on `r.get("type") == qb_type`, so the transaction fixture **must be a separate file** from the entity fixture.
- `pytest.ini` sets `--strict-markers` with exactly one registered marker (`integration`). Add no new marker.
- The repo-wide UUID-literal scan in `tests/test_connectors_api.py` covers `*.py` under `api/`, `dashboard/`, `tests/`. JSON fixtures are exempt; the new test file must carry no UUID literal.
- **Stale in the brief, corrected here:** Postgres HAS shipped (`psycopg[binary]==3.2.3` in `requirements.txt`, `core/graph/pg.py`, a `pg_conn` fixture and an autouse fixture in `tests/conftest.py` that forces `pg_is_available` False for non-`integration` tests). This changes nothing you write: every Postgres instruction in this prompt is a prohibition — add nothing, import nothing, mirror nothing. `git diff requirements.txt` will be empty because psycopg is already committed.

## TASK

Build the transaction writer: create `core/ingestion/transactions.py`, open the fixture-mode transaction read on both V1 connectors, ship the two transaction fixtures with validator checks, and add `tests/test_transaction_ingestion.py`.

## FILE PATHS

**Create:**
- `core/ingestion/transactions.py`
- `tests/fixtures/qb_transactions.json` (QuickBooks transactions fixture)
- `tests/fixtures/ruddr_time_entries.json` (RUDDR time-entries fixture)
- `tests/test_transaction_ingestion.py`

**Modify:**
- `connectors/quickbooks.py` — one new defaulted constructor parameter for the transaction fixture path + one new leading branch in `_fetch_raw_transactions`
- `connectors/ruddr.py` — same, in `_fetch_raw_time_entries`
- `tests/fixtures/generate_fixtures.py` — new referential-integrity checks in `validate_fixtures()`, in the style of its existing cross-file checks

**DO NOT MODIFY:**
- `db/schema_sqlite.sql`, `db/schema.sql`, anything under `db/migrations/` — and add no file there
- `core/ingestion/pipeline.py`, `core/graph/entity_store.py`, `core/matching/scoring.py`, `core/matching/pending_store.py`, `core/matching/engine.py`
- `tests/test_scoring.py`, `tests/test_qb_connector.py`, `tests/test_ruddr_connector.py`, `tests/test_connector_base.py`, `tests/test_pipeline.py`, `tests/test_fixture_loads.py`, `tests/test_schema_parity.py`, `tests/test_connectors_api.py`, `tests/conftest.py`
- `requirements.txt`, `pytest.ini`
- `.claude/settings.json`, anything under `.claude/hooks/` or `.claude/agents/`, `TEMPLATE.md`
- The shipped mapper bodies `_map_transaction` / `_map_time_entry`, and every public connector method signature (`read_transactions` still returns `List[NormalizedTransaction]`)
- Guard condition (2) — `http_client is None` → return empty — in both connectors

## CONVENTIONS

- Python 3.10+ via the project venv. `snake_case` functions, `PascalCase` classes, `UPPER_CASE` constants.
- `core/ingestion/transactions.py` is **module-level functions only** — no class wrapper. `conn: sqlite3.Connection` is the first parameter; `tenant_id` is positioned per the shipped store-layer idiom (`run_ingestion(connector, conn, tenant_id, ...)`).
- **Never call `conn.commit()` or `conn.rollback()` in the new module** — the caller owns the transaction boundary, exactly as `entity_store.py`, `training_data.py` and `pending_store.py` do.
- **Never open a connection** — no `sqlite3.connect` in the new module. Callers supply the connection; production callers get it from `core.matching.pending_store.get_connection`.
- **Idempotency idiom: SELECT on the unique key, then UPDATE in place or INSERT.** `INSERT OR REPLACE` is forbidden, and so is any `ON CONFLICT` clause whose tenant component can be NULL — SQLite treats NULLs as distinct in a UNIQUE index, so such an upsert would silently duplicate on every run.
- Import `AMOUNT_TOLERANCE_PCT` / `AMOUNT_TOLERANCE_CAP` from `core/graph/entity_store.py`. Never re-declare them.
- No bare `except Exception` in the new module — write guards must be explicit and specific, since a broad clause passes vacuously on empty input.
- Tests build their schema **from files** (`db/schema_sqlite.sql` then every `*_sqlite.sql` under `db/migrations/`, sorted), using the `REPO_ROOT / "db" / ...` path-constant + `executescript` pattern from `tests/test_fixture_loads.py`. Never from Python DDL. Derive column names with `PRAGMA table_info`, the migration list from the directory listing, and fixture contents from the fixture files — do not transcribe them.
- Every count, subset or loop assertion is preceded by a non-emptiness assertion on the derived set.
- Fixtures follow the shipped style: top-level JSON list of flat, source-shaped records with `id` as the source-system identifier.
- No UUID literals in the new test file — generate identifiers with `uuid.uuid4()`.
- Feature branches only; never commit to `main`. Log writes (SHIPPED.md, DEBUG.md, RUN_LOG.md, PROMPT_LOG.md, CC-LEARNINGS.md) are append-only.

**Module contract for `core/ingestion/transactions.py`:**

| Symbol | Contract |
|---|---|
| `derive_period(txn_date) -> str` | The 8b rule: a `'YYYY-MM'` bucket derived **from `txn_date`**, never from insert/load/clock time. Raises on a value from which no bucket derives; never substitutes the current date. This is the single non-test implementation — no call site re-derives a month prefix inline. |
| `upsert_transaction(conn, txn, tenant_id) -> int` | Persist one `NormalizedTransaction`, return the `txn_id` of the new or pre-existing row. Keyed on `(tenant_id, source, external_source_id)`. Existing row → UPDATE `category, txn_type, amount, currency, txn_date, period, counterparty_source_id, canonical_id` in place, preserving `txn_id` and `created_at`. No row → INSERT. Requires a non-empty `tenant_id`; raises `ValueError` otherwise. |
| `ingest_transactions(connector, conn, tenant_id, date_range) -> TransactionIngestionSummary` | Call `connector.read_transactions(date_range)` and upsert every element. One connector per call, mirroring `run_ingestion`. Requires a non-empty `tenant_id`; raises `ValueError` otherwise. |
| `TransactionIngestionSummary` | Frozen dataclass. Buckets mutually exclusive and exhaustive: `read_total == inserted + updated + skipped`. Same discipline `IngestionSummary` already states. |

**Counterparty derivation** — one documented module-private helper, applied uniformly:
1. When `NormalizedTransaction.counterparty_kind` already names a customer- or client-side role, carry `counterparty_source_id` through verbatim (the QuickBooks Invoice/Payment path).
2. When `counterparty_kind` names a role that is not the party a receivable is owed by — RUDDR time entries carry the *resource* who logged the hours — fall back to the client reference on `raw_record`, under the client-id key the RUDDR fixture ships. Do **not** rewrite the connector mappers to do this; the write layer owns the interpretation.
3. When neither yields a value, write the row with a NULL `counterparty_source_id` and count it in the summary. Such a row is inert for B3 by construction. It is never silently dropped.

**`canonical_id`** — after deriving the counterparty reference, look `(source, counterparty_reference)` up against `system_references` (single-valued under its `UNIQUE (source, external_id)`), tenant-scoped by joining back to `canonical_entities`, and write the resolved `canonical_id`. When nothing resolves, write NULL and still persist the row. **No fuzzy matching, no alias search, no Stage 1–5 invocation, no entity creation.** The lookup lives as a module-private reader in the new module — do not widen `entity_store.py`'s public surface.

**Write guards** — skip (and count in `skipped`) only when the row cannot satisfy the shipped `NOT NULL` columns: an empty `source_id`, or a `txn_date` from which no period derives.

**Connector fixture-mode guard chain** (both connectors, in this order):
1. transaction fixture path set → load that JSON list via the connector's existing private fixture loader (same `ConnectorError` on missing / non-list), filter to the inclusive `DateRange`, return the raw records so the shipped mapper runs unchanged;
2. entity `fixture_path` set and no transaction fixture → return empty, **byte-for-byte today's behaviour**;
3. `http_client is None` → return empty, unchanged;
4. otherwise → the live API path, unchanged.

The QuickBooks branch must preserve the per-type tagging the mapper reads (a fixture record carrying `"type": "Invoice"` maps correctly with no tagging; stamping `_txn_type` also works — either is fine).

**Fixture cross-linking (load-bearing):** counterparty references in the QB fixture must resolve to `id`s present in `qb_entities.json`; the RUDDR fixture's client ids must resolve into `ruddr_entities.json`. At least one `canonical_ground_truth.json` entity must have same-period, same-currency, within-tolerance amounts on **both** sides, with the tolerance evaluated using the shipped constants. Every date must be chosen so periods align across the two sources — a fixture whose two sides never share a period makes every B3 criterion pass vacuously.

**Named tradeoff, do not "improve":** `period` is a calendar-month bucket, so billing lag that pushes a QB invoice into the month after the RUDDR labour it bills is an accepted 8b false negative. Do not widen the window.

## ACCEPTANCE CRITERIA

Every criterion runs via `.venv/bin/python -m pytest`.

1. `from core.ingestion.transactions import derive_period, upsert_transaction, ingest_transactions, TransactionIngestionSummary` succeeds.
2. **Fixtures exist and are non-empty.** Both new fixture files parse as JSON **lists**, and each has `len(...) > 0`. Every later criterion that iterates or counts is preceded by this precondition — no criterion may pass on an empty fixture.
3. **Fixture referential integrity.** The set of counterparty references in the QuickBooks transactions fixture is non-empty, and every member resolves to an `id` present in `qb_entities.json`; the same holds for the RUDDR fixture against `ruddr_entities.json`. Asserted as `set(...)` non-emptiness first, then containment — never as a subset assertion against a possibly-empty left side.
4. **Both connectors now read transactions in fixture mode.** Constructed with a transaction fixture path and **no** `http_client`, `QuickBooksConnector.read_transactions(range)` and `RuddrConnector.read_transactions(range)` each return a list whose length is `> 0`, and every element is a `NormalizedTransaction` whose `source_id` is non-empty, whose `txn_date` is non-empty, and whose `amount` is a finite number. (Length and field content, not an isinstance check alone.)
5. **Empty-return condition (1) closed, condition (2) preserved.** With **only** the entity `fixture_path` set and no transaction fixture, both connectors still return an empty list. With neither fixture and `http_client is None`, both still return an empty list. Both asserted explicitly.
6. **Date-range filtering.** Given a `DateRange` that excludes some fixture rows, the returned count is strictly less than the count returned for a range covering all of them, **and** both counts are `> 0`.
7. **Rows land.** After `ingest_transactions` runs against each connector on a fresh in-memory database loaded from the schema files, `SELECT COUNT(*) FROM transactions` is `> 0`, equals `summary.inserted`, and `summary.read_total == summary.inserted + summary.updated + summary.skipped`.
8. **Period derivation is the 8b rule, asserted as a property.** `SELECT txn_date, period FROM transactions` returns a non-empty row set, and for **every** row the stored `period` equals the `YYYY-MM` bucket of that row's **own** `txn_date`. A second assertion proves the rule is date-derived and not clock-derived: no stored `period` equals the current month unless that row's own `txn_date` falls in it. No date literal from the brief appears in the test.
9. **`derive_period` rejects, never guesses.** Called with an empty string and with a non-date string, it raises; it never returns a bucket derived from the current time. Asserted with `pytest.raises` on the specific exception type, not a bare `except`.
10. **Counterparty is the customer / client reference.** The set of non-NULL `counterparty_source_id` values written from the QuickBooks fixture is non-empty and is a subset of that fixture's customer/vendor references. The set written from the RUDDR fixture is non-empty and consists of **client** ids: the test first asserts the fixture's *resource*-id set is itself non-empty and disjoint from its client-id set, then asserts every written counterparty is a member of the client-id set and none is a member of the resource-id set.
11. **`canonical_id` is set where — and only where — `system_references` resolves it.** With a graph seeded from `canonical_ground_truth.json`, ingestion produces a non-empty set of rows with non-NULL `canonical_id`, and every such row's `(source, counterparty_source_id)` corresponds to a real `system_references` row. Ingesting the same fixtures against an **empty** graph produces the same total row count (itself asserted `> 0`) with **every** `canonical_id` NULL.
12. **NULL upgrades to resolved on re-run.** Ingest against an empty graph (all `canonical_id` NULL, row count `> 0`), then seed `system_references`, then ingest again: the row count is unchanged, the count of non-NULL `canonical_id` rows rises from zero to a number `> 0`, and no `txn_id` changes value.
13. **Idempotency.** Running `ingest_transactions` twice with the same connector, range and tenant leaves `SELECT COUNT(*) FROM transactions` unchanged after the second run, reports `inserted == 0` and `updated > 0` on the second run, and leaves the set of `txn_id` values **identical** across runs (captured before and compared as sets, both non-empty). A `created_at` captured before the second run is unchanged after it.
14. **`INSERT OR REPLACE` is absent.** `grep -in "insert or replace" core/ingestion/transactions.py` returns nothing.
15. **Non-empty tenant is required on the write path.** `upsert_transaction` and `ingest_transactions` raise `ValueError` for `None` and for an empty-string tenant. A separate test proves rows written under two distinct tenants do not collide: both tenants' rows are present, the total is the sum of the two per-tenant counts, and each per-tenant count is `> 0`.
16. **Transaction neutrality.** `grep -n "\.commit(\|\.rollback(" core/ingestion/transactions.py` returns nothing; a test that ingests and then rolls back on the caller's connection leaves `SELECT COUNT(*) FROM transactions` at its pre-call value, which it captured before the call.
17. **B3 stops being zero — the headline criterion.** In one test: seed the graph from the ground-truth fixture, assert `count_amount_cooccurrence_periods(...)` returns `0` for the chosen pair **before** ingestion (pre-state asserted, not assumed), run `ingest_transactions` for **both** connectors under the **same** tenant, assert the resulting row count is `> 0`, then assert the same call now returns a value `>= 1`. A second assertion runs the same pair through `core.matching.scoring`'s pair-scoring entry point and asserts a `BoostEntry` with `signal_id == "B3"` is present in the resulting `signal_breakdown` boosts and that the boosts tuple is non-empty — the +0.20 cap and the tiering thresholds are 8b's and are not re-asserted here. *(Per the reality check: the B3 boost only computes when the pair's base score lands in `[0.70, 0.90)`, and `score_pair` keys B3's source side on `(entity.source, entity.source_id)` — choose the fixture pair to satisfy both. Never edit `scoring.py`.)*
18. **Guards count, they do not swallow.** A fixture record with an empty source id and one with an unparseable date are both reported in `summary.skipped` with `skipped > 0` while `summary.inserted > 0` for the valid remainder; `grep -n "except Exception" core/ingestion/transactions.py` returns nothing.
19. **No bare UUID literals.** `tests/test_connectors_api.py`'s repo-wide UUID-literal scan over `api/`, `dashboard/` and `tests/` still passes; any identifier this feature's tests need is generated with `uuid.uuid4()`.
20. **One engine, one connection provider.** `grep -n "sqlite3.connect" core/ingestion/transactions.py api/ dashboard/` returns no new match: this module never opens a connection, and `api/` / `dashboard/` continue to obtain SQLite solely through `core.matching.pending_store.get_connection`. `grep -rniE "psycopg|DATABASE_URL|postgres" core/ingestion/transactions.py` returns nothing, and `git diff requirements.txt` is empty.
21. **Schema untouched.** `git diff db/` shows no change to `db/schema_sqlite.sql`, `db/schema.sql` or any file under `db/migrations/`, and no file is added there.
22. `.venv/bin/python -m pytest tests/test_transaction_ingestion.py --collect-only -q` reports a **collected count greater than zero** — asserted on the reported count, not the exit code, because an unregistered marker silently deselects everything and still exits 0.
23. `.venv/bin/python -m pytest tests/test_transaction_ingestion.py -x --tb=short` passes.
24. `.venv/bin/python -m pytest tests/test_qb_connector.py tests/test_ruddr_connector.py tests/test_connector_base.py tests/test_scoring.py tests/test_pipeline.py tests/test_fixture_loads.py -x --tb=short` passes **unmodified** — no existing test file is edited to accommodate this feature.
25. `.venv/bin/python -m pytest tests/ -x --tb=short` passes with no regression, and the collected count is greater than or equal to the count collected on the commit before this feature (measure the baseline at build time before you start; no number is transcribed here).

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Bare `pytest` is NOT on PATH, and `.venv/bin/pytest` fails to put the repo root on `sys.path` — every `from core...` / `from connectors...` import then fails to collect. Always use the `python -m` form. Rebuild the venv if needed with `python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt` (system `python3` is 3.9.6 and unsupported).

## NON-GOALS

- **Feature 15's work:** payment matching, AR aging, unbilled-labour computation, aging buckets, invoice↔payment pairing, any dashboard page or API route displaying money. No `core/reconciliation/` package, no `api/routers/reconciliation.py` body. This feature writes rows; 15 reads them.
- **Any change to the `transactions` DDL.** No migration, no column, no index, no edit to `db/schema_sqlite.sql`, `db/schema.sql` or `db/migrations/003_*`. If something here seems to need a column that does not exist, the requirement is wrong, not the schema.
- **Any change to `count_amount_cooccurrence_periods`, `core/matching/scoring.py`, or B3's tiering / tolerance / cap semantics.**
- **Entity resolution of counterparties.** No matcher invocation, no alias lookup, no entity creation, no `canonical_id` backfill by any means other than an exact `system_references` hit.
- **Closing empty-return condition (2).** A connector with no transport and no fixture still returns empty. No live HTTP added, no credential-flow change, no rate-limit change.
- **Multi-connector orchestration, scheduling, incremental sync cursors, watermarks, backfill windows.** One connector, one explicit `DateRange`.
- **Transaction line items.** V1 reads headers only; nothing here changes that.
- **Postgres.** No entry added to `requirements.txt`, no driver imported, no new environment variable read, no mirror into `db/schema.sql`, no edit to `tests/test_schema_parity.py`, no new `integration`-marked test.
- **Any edit to `core/ingestion/pipeline.py`, `core/graph/entity_store.py`, `core/matching/scoring.py`, `core/matching/pending_store.py`, or `tests/test_scoring.py`.** This feature is additive: one new module, two guarded connector branches plus one defaulted constructor parameter each, two new fixtures, fixture-validator additions, one new test file.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and proceed. Execute every step end-to-end: measure the pre-feature collected count, build, write tests, run the test command until green, then commit on the feature branch. Do not stop until the deliverable is complete or you hit the 3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or rocket halts for human review — so build correctly, but do not pause to ask.)