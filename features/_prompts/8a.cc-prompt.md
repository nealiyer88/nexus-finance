The `_adversaries/` directory already has the other 8a files as untracked — I'll output the prompt directly rather than writing to that path since you haven't granted write access there.

Here is the complete CC build prompt for Feature 8a:

---

## SITUATION

Read:
- `.claude/rules/01-nexus-finance-v1.md` — §§1,2,5,6,9,11,12,13 (v4 rules: pre-trained fastText IN SCOPE, Signal Set B spec, confidence thresholds, owner map)
- `core/matching/scoring.py` — full file. Current state: `SignalBreakdown` has 7 fields (`token_sort_ratio`, `token_set_ratio`, `partial_ratio`, `jaro_winkler`, `ngram_jaccard`, `alias_boost_fired`, `abbreviation_bonus_fired`). `_weighted_score` at lines 286–304 adds `shared_person_bonus` + `neighborhood_overlap_bonus` from a `GraphEvidence` object. No `fasttext_cosine`. No `_compute_b_boosts`. No `BoostEntry`.
- `core/matching/weights.py` — full file. `WeightConfig` has 8 fields: 5 string signals + `alias_boost` + `abbreviation_bonus` + `profile_id`. `alias_boost` (0.15) IS in the sum-to-1.0 budget; `abbreviation_bonus` (0.20 for PSA_ACCOUNTING) is NOT — it is a pure additive bonus outside the sum. No `fasttext_cosine`.
- `core/matching/blocking.py` — full file. `generate_candidates` implements 2a (tokenize) → 2b (token index) → 2c (trigram index) → 2d (intra-system filter) → 2e (CANDIDATE_CAP=50). No fastText Stage 2c yet.
- `core/matching/indices.py` — full file. `TokenIndex`, `NgramIndex`, and `_iter_seed_strings(conn, tenant_id)` helper that yields `(canonical_id, value)` from canonical names + aliases. No `EmbeddingIndex`.
- `core/graph/entity_store.py` — full file. `count_shared_person_neighbors` at line 353 and `count_shared_graph_neighbors` at line 395 exist and are in use. NO `get_created_at`. NO `get_external_field`.
- `requirements.txt` — current dependencies; no compress-fasttext.
- `tests/test_scoring.py` — full file. `test_no_xgboost_no_fasttext_no_llm_in_scoring` MUST be removed (fastText is now in scope per v4). `test_weights_sum_to_one` MUST still pass after `fasttext_cosine` is added.
- `tests/test_blocking.py` — full file.
- `db/schema_sqlite.sql` — verify `canonical_entities.created_at` column and `system_references.external_fields` TEXT/JSON column shape.

Skills: None

Current state:
- `core/matching/embeddings.py` — **DOES NOT EXIST** (create)
- `scripts/fetch_fasttext.py` — **DOES NOT EXIST** (create)
- `tests/test_embeddings.py` — **DOES NOT EXIST** (create)
- `models/` directory — **DOES NOT EXIST** (create with `.gitkeep`)
- `EmbeddingIndex` — **DOES NOT EXIST** (add to `indices.py`)
- `_compute_b_boosts` — **DOES NOT EXIST** (author in `scoring.py`; do not confuse with modifying existing code)
- `get_created_at`, `get_external_field` — **DO NOT EXIST** (add to `entity_store.py`)
- `BoostEntry`, `_extract_project_code_fragments` — **DO NOT EXIST** (add to `scoring.py`)

---

## TASK

Add pre-trained fastText (Stage 2c + Signal Set C) and Signal Set B (B1, B2, B4, B5, B6) to the shipped v4 matching stack as two sequential commits on the current branch. Commit 1 lands Stage 2c + Signal C. Commit 2 lands Signal Set B.

---

## FILE PATHS

**Create:**
- `core/matching/embeddings.py`
- `scripts/fetch_fasttext.py`
- `tests/test_embeddings.py`
- `models/.gitkeep`

**Modify:**
- `core/matching/indices.py` — add `EmbeddingIndex` class
- `core/matching/blocking.py` — add Stage 2c (fastText flat-cosine top-k) between existing 2b and 2d
- `core/matching/scoring.py` — extend `SignalBreakdown`, add `BoostEntry` dataclass, add `_extract_project_code_fragments`, author `_compute_b_boosts`, update `_weighted_score` and `score_pair`
- `core/matching/weights.py` — add `fasttext_cosine: float` to `WeightConfig`; adjust weight profiles so sum-to-1.0 still holds
- `core/graph/entity_store.py` — add `get_created_at` and `get_external_field` accessors
- `tests/test_scoring.py` — remove `test_no_xgboost_no_fasttext_no_llm_in_scoring`; update `test_signal_breakdown_carries_every_weighted_signal`; add Signal C and Signal B tests
- `tests/test_blocking.py` — add Stage 2c candidate-surfacing test (monkeypatched embed)
- `requirements.txt` — add `compress-fasttext` pinned to latest stable
- `.gitignore` — add `models/*.bin`, `models/*.gz`, `models/*.bin.gz`

**DO NOT MODIFY:**
- `.claude/settings.json`, `.claude/hooks/`, `.claude/agents/`
- `TEMPLATE.md`
- Any Stage 0, 1, 4, 5, 6 files
- `db/schema.sql` or `db/schema_sqlite.sql` — no schema changes in 8a
- `SHIPPED.md`, `DEBUG.md`, `PROMPT_LOG.md`, `CC-LEARNINGS.md` — append-only; do not touch unless logging a build event

---

## CONVENTIONS

- Python 3.10+; snake_case functions, PascalCase classes, UPPER_CASE constants
- All new dataclasses: `@dataclass(frozen=True)`
- Use `from __future__ import annotations` if `Literal` or forward references require it
- `Optional[str]` from `typing` — match the style in `entity_store.py` (not `str | None`)
- Imports: stdlib → third-party → local, one blank line between groups
- No docstrings; no multi-line comment blocks; one short inline comment only where WHY is non-obvious
- Tenant scoping: every new `entity_store` accessor ends `tenant_id: Optional[str] = None`; when `None`, omit the `tenant_id` WHERE clause — match the pattern of `count_shared_person_neighbors`
- Test fixtures: use SQLite in-memory databases created with `db/schema_sqlite.sql` DDL, matching the pattern in existing test files
- Integration tests requiring the downloaded model: `@pytest.mark.skipif(not _model_present(), reason="fasttext model not downloaded")` where `_model_present()` checks if the model file placed by `fetch_fasttext.py` exists

---

## ACCEPTANCE CRITERIA

### Commit 1: Stage 2c + Signal Set C

**AC-1 — embeddings.py:**
- `embed(name: str) -> tuple[float, ...] | None`: returns `None` for `""` and whitespace-only strings; does not raise
- `cosine(a, b) -> float`: returns `0.0` when either operand is `None`; does not raise
- Model loaded via `compress_fasttext.CompressedFastTextKeyedVectors.load(...)` — pure-Python, no C++ extension

**AC-2 — fetch_fasttext.py:**
- Idempotent: if model file already in `models/`, skip download with a message
- Prints or logs the source URL and SHA256 of the downloaded file
- Safe to invoke in CI with or without network access (model absent → downloads; present → no-ops)

**AC-3 — EmbeddingIndex:**
- `EmbeddingIndex` class in `core/matching/indices.py`
- `EmbeddingIndex.build(conn, tenant_id=None)` classmethod seeds from `_iter_seed_strings(conn, tenant_id)` exactly as `TokenIndex.build` and `NgramIndex.build` do
- `EmbeddingIndex.query(name: str, top_k: int) -> list[tuple[str, float]]`: returns `(canonical_id, cosine_score)` pairs sorted descending, length ≤ `top_k`; returns `[]` if `embed(name)` is `None`

**AC-4 — blocking.py Stage 2c:**
- `generate_candidates` calls `EmbeddingIndex.query` AFTER step 2b (trigram) and BEFORE step 2d (intra-system filter)
- Each Stage 2c hit added with blocking signal `"embed:<rank>"` (1-indexed: `"embed:1"`, `"embed:2"`, etc.)
- Stage 2c candidates UNION-ed with 2a/2b candidates before 2d and 2e; both filters still apply to the combined set
- `generate_candidates` gains `embedding_index: Optional[EmbeddingIndex] = None` parameter; when `None`, Step 2c is skipped (backward-compatible default)

**AC-5 — WeightConfig + profiles:**
- `WeightConfig` gains `fasttext_cosine: float` field
- `fasttext_cosine` is INCLUDED in the sum-to-1.0 budget (same class as `alias_boost`, NOT like `abbreviation_bonus`)
- `DEFAULT_WEIGHTS`: `fasttext_cosine=0.05`; reduce other signal weights proportionally so sum equals exactly `1.0`
- `PSA_ACCOUNTING_WEIGHTS`: `fasttext_cosine=0.12`; reduce other signal weights proportionally so sum equals exactly `1.0`
- `test_weights_sum_to_one` passes without modifying its assertion (only the numeric values change)

**AC-6 — SignalBreakdown (Commit 1):**
- `SignalBreakdown` gains `fasttext_cosine: float` field, default `0.0`
- `score_pair(...)` populates `signal_breakdown.fasttext_cosine` with the raw cosine for every pair; `0.0` when embed unavailable
- `test_signal_breakdown_carries_every_weighted_signal` updated to assert `fasttext_cosine` is present

**AC-7 — test_no_xgboost_no_fasttext_no_llm_in_scoring removed:**
- Deleted from `tests/test_scoring.py`. fastText is IN SCOPE per v4 rules §1.

**AC-8 — test_embeddings.py minimum tests:**
- `test_embed_empty_returns_none`
- `test_embed_whitespace_returns_none`
- `test_cosine_none_operand_returns_zero`
- `test_cosine_identical_returns_one` (monkeypatched embed returning identical unit vectors)
- `test_cosine_orthogonal_returns_zero` (monkeypatched embed returning orthogonal vectors)

**AC-9 — blocking Stage 2c test:**
- New test in `tests/test_blocking.py`: seed `EmbeddingIndex` with `"pacific rim technologies international"` via monkeypatched `embed`; query `"pacrim tech"`; assert returned `CandidateSet` contains that canonical with blocking signal matching `"embed:*"` pattern

**AC-10 — Integration test (Signal C lift, model-gated):**
- One test in `tests/test_scoring.py` decorated `@pytest.mark.skipif(not _model_present(), reason="fasttext model not downloaded")`
- With `PSA_ACCOUNTING_WEIGHTS` (`fasttext_cosine=0.12`): both `"meridian cap"↔"meridian capital group"` and `"pacrim tech"↔"pacific rim technologies international"` score **≥ 0.70**
- With `fasttext_cosine` weight set to `0.0`: both pairs score **< 0.70** (proves Signal C is load-bearing)

**AC-11 — requirements.txt:**
- Exactly one new pinned dependency: `compress-fasttext==<version>`. No other new packages in Commit 1.

**AC-12 — .gitignore + models/:**
- `.gitignore` contains `models/*.bin`, `models/*.gz`, `models/*.bin.gz`
- `models/.gitkeep` committed so the directory exists in the repo

---

### Commit 2: Signal Set B

**AC-13 — BoostEntry dataclass:**
```python
@dataclass(frozen=True)
class BoostEntry:
    signal_id: Literal["B1", "B2", "B4", "B5", "B6"]
    raw: float
    applied: float
```
Lives in `core/matching/scoring.py`. `from typing import Literal` imported.

**AC-14 — _compute_b_boosts authored:**
- `grep "def _compute_b_boosts" core/matching/scoring.py` returns exactly one match
- Signature: `_compute_b_boosts(conn, source_id: str, candidate_id: str, source_category: str, candidate_category: str, source_external_fields: dict, candidate_external_fields: dict, base_score: float, tenant_id: Optional[str] = None) -> tuple[BoostEntry, ...]`
- Returns `()` when `base_score < 0.70` or `base_score >= 0.90`
- Calls each of B1, B2, B4, B5, B6 to compute raw boosts; collects only those where raw > 0
- Sums raw boosts; caps total at `+0.20`; distributes cap proportionally: `applied = raw * min(1.0, 0.20 / total_raw)` when total > 0.20, else `applied = raw`
- Returns one `BoostEntry` per fired signal (raw > 0)

**AC-15 — B3 absent:**
- `grep -r "amount_cooccurrence\|transaction" core/matching/scoring.py` → zero matches
- `Literal["B1","B2","B4","B5","B6"]` on `BoostEntry.signal_id` prevents B3 population at type level

**AC-16 — B1 (shared person, +0.05/person, cap +0.10):**
- Calls `count_shared_person_neighbors(conn, source_id, candidate_id, tenant_id)` (entity_store.py:353)
- `raw = min(n * 0.05, 0.10)`

**AC-17 — B2 (project-code fragment, +0.08–0.12):**
- `_extract_project_code_fragments(external_fields: dict) -> set[str]` in `scoring.py`
- Reads keys `"class"`, `"memo"`, `"project_codes"` (list when present); splits each value on `-`, `/`, `_`, ` `; uppercases; keeps fragments ≥ 2 chars
- B2 fires when `_extract_project_code_fragments(source_ext) ∩ _extract_project_code_fragments(candidate_ext)` is non-empty
- `raw = 0.08` for 1 overlapping fragment; `raw = 0.12` for ≥ 2
- Unit test positive: matching `"class"` fragments → B2 fires, `raw in {0.08, 0.12}` (asserted independently of the cap)
- Unit test negative: no overlapping fragments → B2 does NOT fire

**AC-18 — B4 (email domain, +0.05–0.08):**
- Calls `get_external_field(conn, id, "email", tenant_id)` for each entity
- Splits on `@`; compares domain case-insensitively
- `raw = 0.05` for freemail domains (`gmail.com`, `yahoo.com`, `hotmail.com`, `outlook.com`); `raw = 0.08` for all other domains
- Does not fire when either entity has no email in external_fields

**AC-19 — B5 (temporal co-occurrence, +0.03–0.05):**
- Calls `get_created_at(conn, id, tenant_id)` for both entities
- Fires only when both are non-None and `abs((a - b).days) <= 30`
- `raw = 0.05` when delta < 15 days; `raw = 0.03` when 15 ≤ delta ≤ 30

**AC-20 — B6 (neighborhood overlap, +0.025/node, cap +0.10):**
- Calls `count_shared_graph_neighbors(conn, source_id, candidate_id, tenant_id)` (entity_store.py:395)
- `raw = min(n * 0.025, 0.10)`

**AC-21 — New entity_store accessors:**
- `get_created_at(conn, canonical_id: str, tenant_id: Optional[str] = None) -> Optional[datetime]` — `SELECT created_at FROM canonical_entities WHERE canonical_id = ?` (plus tenant filter when set); returns parsed `datetime` or `None`
- `get_external_field(conn, canonical_id: str, field_name: str, tenant_id: Optional[str] = None) -> Optional[str]` — queries `system_references.external_fields` JSON for `field_name`; returns first non-None hit across all system_references rows for the canonical; same defensive pattern as `lookup_email` (LIKE perf hint, then Python JSON parse)

**AC-22 — SignalBreakdown extension (Commit 2):**
- `SignalBreakdown` gains `b_boosts: tuple[BoostEntry, ...]` field, default `()`
- `score_pair(...)` calls `_compute_b_boosts` and stores result in `signal_breakdown.b_boosts`

**AC-23 — _weighted_score updated:**
- Adds `fasttext_cosine * weights.fasttext_cosine` to the weighted sum (already in from Commit 1)
- Replaces old `graph_evidence.shared_person_bonus + graph_evidence.neighborhood_overlap_bonus` additions with `sum(e.applied for e in b_boosts)` (already-capped total from `_compute_b_boosts`)
- `alias_boost` and `abbreviation_bonus` (Set A) remain additive and are NOT subject to the +0.20 Set B cap
- Keep `GraphEvidence` dataclass and `ScoredMatch.graph_evidence` field; populate `shared_person_count` from B1's raw boost ÷ 0.05 and `neighborhood_overlap_count` from B6's raw boost ÷ 0.025, so the `approval_decisions.graph_evidence` JSONB column remains populated

**AC-24 — Cap test:**
- New test in `tests/test_scoring.py`: monkeypatch `count_shared_person_neighbors` → 2 (B1 raw=0.10), `_extract_project_code_fragments` → overlapping set ≥ 2 fragments (B2 raw=0.12), `get_external_field` for email → matching corporate domain (B4 raw=0.08), `get_created_at` → 5-day delta (B5 raw=0.05); base_score=0.80 (inside ambiguous band)
- Assert `sum(e.applied for e in result.signal_breakdown.b_boosts) == pytest.approx(0.20, abs=1e-9)`
- Assert `len(result.signal_breakdown.b_boosts) == 4`
- Assert each `e.applied <= e.raw`

**AC-25 — Negative-match test:**
- `"brightpath machine learning"` (source_category="accounting") vs `"luminos ai"` (candidate_category="psa") scores < 0.50
- Use real `score_pair` with an in-memory SQLite DB fixture in `tests/test_scoring.py`

**AC-26 — B-signal gating:**
- `_compute_b_boosts` is called in `score_pair` after the base string+embedding score is computed; that raw score is passed as `base_score`
- When `base_score < 0.70` or `base_score >= 0.90`, function returns `()` and adds no boost

---

## TEST COMMAND

```
pytest tests/ -x --tb=short
```

Run after EACH commit before writing the commit message. Both commits must be green before the branch is considered complete.

---

## NON-GOALS

- B3 (amount co-occurrence) and any transactions table — Feature 8b. Do NOT scaffold, stub, import, or reference in any file, comment, or test.
- Fine-tuned fastText, ANN library (faiss/annoy/hnswlib), contextual embeddings.
- XGBoost, GraphRAG, self-hosted LLM, write-back.
- Persistence of `EmbeddingIndex`; no `update()` method.
- Changes to Stages 0, 1, 4, 5, 6 files or interfaces.
- Raising or lowering disposition cutoffs (0.90/0.70/0.50 unchanged). The 95% auto-match gate is measured by Feature 12.
- Modifying `db/schema.sql` or `db/schema_sqlite.sql`.
- Making `fetch_fasttext.py` download during CI — the integration test is skipif-gated on model presence.

---

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)

---

That's the complete prompt. Key things it does that prior attempts didn't:

- **Grounded in actual file state**: references exact line numbers (`entity_store.py:353`, `entity_store.py:395`), existing test names to remove/update, and fields that already exist vs. those being added
- **Distinguishes author vs. modify**: `_compute_b_boosts` is explicitly flagged as a new function to *author*, not a modification — addressing the engineer flag about it not existing
- **Resolves the weight-sum invariant**: `fasttext_cosine` is in the sum-to-1.0 budget (like `alias_boost`), with explicit target values so the builder knows which profile gets 0.05 vs. 0.12 and must reduce others proportionally
- **B2 utility contract is locked**: `_extract_project_code_fragments` has a precise input/output spec and positive+negative unit tests that are cap-independent
- **B3 guard is structural + grep**: both `Literal["B1","B2","B4","B5","B6"]` and a grep command in AC-15
- **GraphEvidence backward compat**: keeps it populated from B1/B6 so `approval_decisions.graph_evidence` JSONB doesn't break