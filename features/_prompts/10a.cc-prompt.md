Build prompt for feature 10a:

```markdown
## SITUATION

Read before writing any code:
- `CLAUDE.md` (repo root) — test command, venv rules, append-only log rule, feature-branch rule.
- `.claude/rules/01-nexus-finance-v1.md` — §0 (BUILT vs PLANNED), §10 (data security posture / credential handling), §12 (owner map).
- `features/pipeline/postgres-bootstrap.md` (this feature's brief, feature #10a) if present in the tree.
- `db/schema.sql` — the `[PLANNED]` Postgres schema; read the `approval_decisions` table's `disposition` CHECK constraint.
- `db/migrations/` — list the whole directory; read the SQLite-dialect pending-decisions migration's `status` CHECK constraint.
- `core/matching/llm_fallback.py` — copy the existing lazy, guarded `load_dotenv` convention from the function that calls it (comment + `from dotenv import load_dotenv as _load_dotenv` inside the function body).
- `tests/test_schema_parity.py` — read its shared-table literal; do not modify it.
- `tests/test_resolution.py` and `tests/test_pending_decisions.py` — locate the requirements-diff guard tests by grep.

Skills: `.claude/skills/ROCKET-SKILLS.md` lists the project's harness skills; `security-triage` covers secrets-scan findings if the secrets gate fires. Do not paste skill contents anywhere.

Current state (verified against the tree at `08773aa`):
- **No pytest configuration of any kind exists** — no `pytest.ini`, `setup.cfg`, `tox.ini`, `pyproject.toml`, or `conftest.py`. Verify this yourself before creating one.
- `core/graph/` contains only `__init__.py`, `entity_store.py`, `resolution.py`. `pg.py` and `dispositions.py` do not exist.
- `scripts/` has no `migrate_pg.py`. `db/migrations/` contains only `*.sql` — no manifest.
- No Postgres driver is pinned. `supabase==2.9.0` sits under the `# Database` comment block in `requirements.txt` and is never imported. `python-dotenv` is already pinned — add no loader pin.
- `.env.example` has no `DATABASE_URL` line. Its style is `# Section` headers followed by `KEY=` lines, some empty, some with non-secret defaults.
- `.env` is gitignored and untracked.
- `core.matching.types.Action` is a `typing.Literal` of Stage-4 routing bands — not an `Enum`, no member iteration, no `terminal` attribute. Do NOT key the disposition mapping on it.
- Two requirements-diff guard tests exist, both named `test_requirements_txt_unchanged` — one in `tests/test_resolution.py`, one in `tests/test_pending_decisions.py`. Re-derive the set by grep rather than trusting this line; a later feature may have added another.
- Feature 10's Postgres-token grep-guard (`test_no_postgres_references`) scans a fixed tuple of modules that does **not** include a new `core/graph/pg.py` — leave that guard untouched; adding `pg.py` does not trip it. 10b's equivalent guard is likewise unaffected.
- No outbound-client timeout convention exists in `core/`, `connectors/`, or `api/`. The only `*_TIMEOUT` in the tree is a subprocess probe timeout in harness tooling — not a client convention. Confirm by grep, then define your own named constant per CONVENTIONS below.
- Baseline: the full suite currently passes clean with `DATABASE_URL` unset. Capture the actual collected count from a run at build time — do not write any count into a test.

**No database is required to build, run, or verify this feature. Nothing here opens a connection. `DATABASE_URL` stays unset for every test run.**

## TASK

Build the offline half of the Postgres path: pin the driver, add `core/graph/pg.py`, `core/graph/dispositions.py`, `pytest.ini`, `scripts/migrate_pg.py`, and `db/migrations/postgres.manifest`; retire the requirements-diff guards in the same commit as the pin; write the non-integration tests for all of it.

## FILE PATHS

Create:
- `pytest.ini` (repo root)
- `core/graph/pg.py`
- `core/graph/dispositions.py`
- `scripts/migrate_pg.py`
- `db/migrations/postgres.manifest`
- Test file(s) under `tests/` for this feature's own criteria (follow the existing `tests/test_*.py` naming)

Modify:
- `requirements.txt` — add exactly one Postgres driver pin, `psycopg[binary]==<pin>`, under the existing `# Database` comment block. Remove no line.
- `.env.example` — add a `DATABASE_URL=` line with an obviously-fake placeholder, in the file's existing commented-section style.
- `tests/test_resolution.py` — **delete** the requirements-diff guard test. Nothing else.
- `tests/test_pending_decisions.py` — **delete** the requirements-diff guard test. Nothing else.
- Any other test file a build-time grep of `tests/` shows asserting the `requirements.txt` working-tree diff is empty — delete that test too.

DO NOT MODIFY:
- `TEMPLATE.md`, `.claude/settings.json`, anything under `.claude/hooks/` or `.claude/agents/`
- `db/schema.sql` (feature 2 owns type changes)
- Any shipped file under `db/migrations/` — leave every `.sql` byte-unchanged
- `tests/test_schema_parity.py` (and its shared-table literal)
- `core/graph/resolution.py`, `core/matching/training_data.py`, feature 10b's store module and migration
- Feature 10's `test_no_postgres_references` guard and 10b's `test_no_postgres_or_forbidden_references` guard — both survive
- Any test in `tests/test_resolution.py` / `tests/test_pending_decisions.py` other than the named requirements-diff guard

## CONVENTIONS

- Python 3.10+ via the project venv. `snake_case` functions, `PascalCase` classes, `UPPER_CASE` constants.
- **The database target is supplied entirely by the environment.** No module, test, comment, fixture, docstring, or printed line may contain a host, port, database name, or username; may build a DSN out of parts; or may assume the DSN carries a password component. Never invent a default DSN. Minimum Postgres major version 17 — a minimum major, never a pinned patch, anywhere.
- **Absence of `DATABASE_URL` means "Postgres not available", never an error.** `get_dsn()` returns `None` and never raises.
- **`DATABASE_URL` is a live credential at all times**, whether or not its value happens to carry a password, and no code path may branch on which. It is never logged at any level, never printed, never echoed in an error message (`connect()` may name the *variable*, never its *value*; driver exceptions embedding connection parameters are caught and re-raised scrubbed), and never written to any file under `features/` or `.rocket/`, nor into a fixture, snapshot, log, migration, or committed config.
- **`.env` loading reuses the existing lazy, guarded import convention** found in `core/matching/llm_fallback.py` — the import lives inside the function that needs it, wrapped so a missing package does not break module importability. Add no new loader and no new `requirements.txt` line for one.
- **Every connection carries an explicit connect timeout**, sourced from a single `UPPER_CASE` module-level constant in `core/graph/pg.py`, overridable by an environment variable. Never an inline numeric literal at the call site. No code or test may assume any particular connect or query latency.
- No pooling, no ORM, no engine abstraction, no async, no vendor SDK. One driver, one `connect()`. `supabase==2.9.0` stays unimported.
- **Cite symbols, not coordinates.** No `file.py:NNN` references, no counts, no "exactly N" claims about the tree in any code, comment, test, or log. Every quantitative check is an invariant computed at build or test time via glob/grep/set comparison.
- All log writes (`SHIPPED.md`, `DEBUG.md`, `RUN_LOG.md`, `PROMPT_LOG.md`, `CC-LEARNINGS.md`) are append-only. Feature branches only — never commit to `main`.

### Module shapes

`core/graph/pg.py` — importable with no database present:
- `get_dsn() -> str | None` — reads `DATABASE_URL`, loading `.env` first via the lazy-guarded loader convention. Returns `None` when unset. Never raises, never invents a default.
- `connect() -> psycopg.Connection` — opens a connection carrying the explicit connect timeout from the named constant. Raises `RuntimeError` naming the variable `DATABASE_URL` when the DSN is absent; the message never contains the value.
- `is_available() -> bool` — the single availability helper. Used by test skip guards, and in 10c by the Stage 6 call site, which reaches Postgres through nothing else.
- `BOOTSTRAP_TENANT_ID` — a fixed UUID **literal** at module level, not generated (no `uuid4()` anywhere in the file), importable with no database present. Feature 16 will bind `DEFAULT_TENANT_ID = BOOTSTRAP_TENANT_ID` and assert identity — do not rename it, relocate it, database-couple it, or make it computed.

`pytest.ini` — exactly:
```ini
[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    integration: requires a reachable Postgres via DATABASE_URL
```
Adding this fixes rootdir at the repo root and changes collection for a suite that has never had a config file, so re-verify the full existing suite under it. Note: rootdir is *not* what puts the repo root on `sys.path` — that comes from invoking `.venv/bin/python -m pytest`.

`db/migrations/postgres.manifest` — newline-delimited bare filenames; blank lines and `#` comment lines ignored. **Authoring rule, resolved at build time:** enumerate `db/migrations/*.sql` and list every file whose name does **not** end in `_sqlite.sql`, in ascending filename order. 10c and feature 16 append theirs at the end — preserve append-at-end semantics. 10b's pending-decisions table gets **no** Postgres mirror and **no** manifest entry.

`scripts/migrate_pg.py`:
- **SELECTION RULE — the one mechanism, no other rule applies:** the runner applies exactly the filenames listed in the manifest, in the order they appear there, and never reads or applies any other file in `db/migrations/`. If the manifest names a file absent from disk, the runner exits non-zero, naming the missing filename, before applying anything and before opening any connection.
- Creates `schema_migrations (filename TEXT PRIMARY KEY, applied_at TIMESTAMPTZ NOT NULL DEFAULT now())` before applying anything; **skips any file already recorded**, so a file is applied at most once ever. Prints one line per file (`applied` / `skipped`), exits 0 on success. **No printed line contains the DSN on any path, including failures.** Supports `--dry-run`, which prints the plan and applies nothing.
- **DESTRUCTIVE-FILE GUARD — the primary safety mechanism.** Before applying the canonical-schema migration (identify it at build time as the manifest entry whose file creates `canonical_entities`), the runner checks whether `canonical_entities` already exists while `schema_migrations` holds no row for that filename. If so it refuses to run, exits non-zero, and prints the reason. Assume the target holds data someone cares about; never treat it as disposable.
- **Idempotency is a runner-level property only:** a second run applies zero files and exits 0. Never claim any shipped migration is safe to re-execute — the canonical-schema migration opens, inside a single `BEGIN`, with a block of unconditional `DROP TABLE IF EXISTS ... CASCADE` statements; `IF EXISTS` suppresses the absent-table error, it does not make the statement conditional. Do not rewrite any `DROP` block — that belongs to the owning schema feature.
- **Module docstring must flag the data-loss hazard prominently**, containing the literal words `DROP TABLE` and `data loss`, and stating that running a destructive migration by hand, or deleting its `schema_migrations` row and re-running, destroys all data in the tables that migration drops, with no escape hatch and no automatic snapshot. It must **name every migration file containing an unconditional `DROP TABLE`**, derived at build time by `grep -lE '^DROP TABLE' db/migrations/*.sql` excluding `*_sqlite.sql`. Derive the set; do not copy one from anywhere.

`core/graph/dispositions.py` — a `MAPPING` dict, no database required. 10c's approvals writer imports it.
- **The terminal vocabulary is decided and not to be re-litigated:** it is the value list of the `status` CHECK constraint in the SQLite-dialect pending-decisions migration under `db/migrations/`, **minus that constraint's single non-terminal "still pending" value**. Locate the migration by **listing `db/migrations/` at build time** — never by a filename written into code, a test, or a comment. Parse the CHECK's value list out of that file; that parse is the authority. Do **not** key on `core.matching.types.Action`.
- The allowed values come from `approval_decisions.disposition`'s CHECK in `db/schema.sql`, read **by grep at test time**, never as a literal list.
- **Keys and values are different obligations:** `set(MAPPING.keys()) == TERMINAL_SET` is a **set EQUALITY**; `set(MAPPING.values()) <= CHECK_SET` stays a **subset** assertion. Both right-hand sides are derived at run time. **No producer-set assertion of any kind exists anywhere in this feature** — producer presence is irrelevant to this mapping.
- **The mapping COVERS the full terminal set.** Every terminal value is a key. There is no exclusion rule of any kind — nothing is left out for having no in-tree producer, and no producer may be invented.
- **This is an identity mapping.** The two derived sets coincide in this repo, so each terminal value maps to the identically-spelled CHECK value. Confirm the coincidence at build time by comparing the two derived sets, not by transcription. Add no translation, aliasing, or case-normalization layer. **No value from either set is transcribed as a literal into code or into a test.**

### Guard-retirement rule

The requirements-diff guards assert against the **working tree**, and the gate runs **pre-commit against the working tree**. Adding the pin and keeping those guards cannot both hold, so:
- Retire every shipped test that asserts the `requirements.txt` diff is empty, **in the same commit** that adds the pin. Derive the set at build time by grepping `tests/` for tests that shell out to `git diff` against `requirements.txt` and assert emptiness.
- Each retired test is **deleted outright — never skipped**. A skipped guard still matches the criterion's grep. The retired set is enumerable from the diff alone. **Write no count of retired tests anywhere** — not in a test, a comment, or a log.
- Committing the pin separately to launder the diff to empty is explicitly **forbidden**.

## ACCEPTANCE CRITERIA

**Marker registration (no database required):**

1. `pytest.ini` exists at repo root and registers `integration`; `.venv/bin/python -m pytest --markers` output contains a line starting `@pytest.mark.integration`.
2. With `--strict-markers` active, a test decorated with a deliberately bogus marker causes a **collection error**, asserted in a subprocess test expecting a non-zero exit.
3. `.venv/bin/python -m pytest tests/ -m "not integration" -x --tb=short` exits 0 with `DATABASE_URL` unset **and reports a collected count greater than zero**, parsed from the same run's output.
4. `.venv/bin/python -m pytest tests/ -x --tb=short` exits 0 under the new `pytest.ini`, and its collected count is **greater than or equal to** the count from the same command on the prior commit — both captured at build time from the runs themselves. No literal count is written into any test, and no count is expected to *decrease*: this feature retires guards but also adds test files.

**Guard retirement (no database required):**

5. Every test in feature 10's shipped suite that this feature does not explicitly retire passes under `.venv/bin/python -m pytest tests/test_resolution.py -x --tb=short`, with `DATABASE_URL` unset, and its collected count is greater than zero. The retired set is enumerable from the diff — each retired test is **deleted**, never skipped. No count of retired tests appears in any test or comment.
6. The same holds for feature 10b's suite: `.venv/bin/python -m pytest tests/test_pending_decisions.py -x --tb=short` passes for every test not explicitly retired, with a collected count greater than zero.
7. **No requirements-diff guard remains that contradicts the pin:** a test greps `tests/` for any test asserting the `requirements.txt` diff is empty and asserts the result set is empty. Derived by grep, never against an expected number.
8. **REVIEWER / CHECKLIST OBLIGATION — not a pytest assertion.** The driver pin and the guard retirements must land in the **same commit**. The reviewer confirms, at review time, that the commit's changed-path set contains both `requirements.txt` and every file that held a retired guard. This is deliberately a human step: the gate runs pre-commit against the working tree, where that commit does not yet exist, so no test in this suite can inspect it — do not write one. The substance is unchanged and unconditional: pin and retirements land together, and committing the pin separately to launder the diff is forbidden.

**Driver + connection (no database required):**

9. The `requirements.txt` diff adds a Postgres driver pin matching `^psycopg\[binary\]==` and **removes no line** — the added-line set is non-empty, every added line matches that pattern, and the removed-line set is empty. `.venv/bin/python -c "import psycopg"` succeeds after `pip install -r requirements.txt`. A grep for any second Postgres driver pin matches nothing.
10. `.env.example` contains a `DATABASE_URL=` line whose value is a placeholder: a test asserts it does not parse as a usable credential and that every component it carries is obviously synthetic. The test asserts nothing about which components are present. `.env` remains gitignored and untracked (`git check-ignore .env` succeeds; `git ls-files .env` returns nothing).
11. With `DATABASE_URL` unset: `core.graph.pg.get_dsn()` returns `None`, `is_available()` returns `False`, and `connect()` raises `RuntimeError` whose message contains the literal string `DATABASE_URL` and **does not** contain any DSN value.
12. **Connect timeout is present and named:** a grep asserts `connect()` passes an explicit connect-timeout argument, that its value is a module-level `UPPER_CASE` constant in `core/graph/pg.py`, and that no inline numeric literal appears at the call site. No timeout value is asserted — only that one is set from a named constant.
13. `core.graph.pg.BOOTSTRAP_TENANT_ID` imports **with no database present**, parses as a valid UUID, and is a module-level literal (no `uuid4()` call in `core/graph/pg.py` — grep-asserted), so feature 16's `DEFAULT_TENANT_ID` can bind to it by identity.

**Migration runner and manifest (no database required):**

14. **Manifest covers every Postgres migration on disk:** the set of non-`_sqlite` `.sql` files under `db/migrations/` equals the set of manifest entries — a set comparison computed at test time. Grep-assert the manifest contains no `_sqlite` entry.
15. **Prefix invariant:** no two files in `db/migrations/` share a numeric prefix unless one is the other's `_sqlite` dialect sibling — computed by listing the directory at test time. (The next free prefix, if one is ever needed, is computed over the **whole** directory, `_sqlite` siblings included — a SQLite-only migration with no counterpart owns its prefix outright.)
16. `scripts/migrate_pg.py`'s module docstring contains the literal words `DROP TABLE` and `data loss` — grep-asserted.
17. **The docstring names every destructive migration, derived not hardcoded:** a test computes the set of files under `db/migrations/` (excluding `*_sqlite.sql`) matching `^DROP TABLE`, and asserts every filename in that set appears verbatim in the module docstring.
18. The manifest parser is unit-tested directly with `DATABASE_URL` unset: blank lines and `#` comments are ignored, order is preserved, and a manifest entry naming a nonexistent file makes the runner exit **non-zero** naming the missing filename before opening any connection.
19. The parity test's shared-table literal is unchanged and still omits 10b's table — asserted by importing the parity module.

**Disposition mapping (no database required):**

20. A test parses the CHECK set out of `db/schema.sql` by grep, parses `TERMINAL_SET` out of the `status` CHECK in 10b's pending-decisions SQLite migration (located by listing `db/migrations/`, never by filename) minus the non-terminal value, then asserts `set(MAPPING.keys()) == TERMINAL_SET` (**equality** — an empty or partial mapping fails here) **and** `set(MAPPING.values()) <= CHECK_SET` (subset). No set appears as a literal in the test.
21. **Non-vacuity:** a separate assertion requires `MAPPING` to be non-empty, standing on its own so no future edit can hollow the mapping out behind a vacuously-true subset check.
22. **Identity, derived not transcribed:** the same test asserts the two parsed sets coincide and that every key maps to a value equal to itself — confirming the mapping is the identity and that no translation layer was introduced.

**Secret hygiene (no database required):**

23. **The DSN never reaches logs or errors:** a grep over `core/graph/pg.py` and `scripts/migrate_pg.py` finds no path that logs, prints, or formats `get_dsn()`'s return value or the raw environment value into a message. A unit test monkeypatches `DATABASE_URL` to a sentinel, exercises every failure path reachable without a database (missing DSN, malformed DSN, connect failure), captures stdout/stderr and the exception messages, and asserts the sentinel appears in none of them.
24. **Scoped to this feature's own diff.** Across the lines **this feature adds** under `features/` and `.rocket/` — derived from this feature's commit diff, added lines only — no added line assigns a value to `DATABASE_URL`. The criterion deliberately does not grep the repo: other briefs already contain illustrative `DATABASE_URL=` text on a clean checkout, so a repo-wide grep would fail on arrival. The substance is unconditional: the connection string is never logged, never placed in an error message, and never written to any file under `features/` or `.rocket/`; the only committed assignment carrying a value anywhere is the obviously-fake placeholder in `.env.example`. When `DATABASE_URL` is unset the check runs against the sentinel and still asserts absence.

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Bare `pytest` is NOT on PATH, and `.venv/bin/pytest` fails to put the repo root on `sys.path` — every `from core...` / `from connectors...` import then fails to collect. Use the `python -m` form for every run, including the per-file and `-m "not integration"` runs in the criteria.

Before adding `pytest.ini`, capture the baseline collected count by running the command above on the current working tree (or the prior commit) — you need it for criterion 4. Do not write that number into any file.

## NON-GOALS

- Executing migrations against a live server, tenant provisioning, the writers, the Stage 6 call site, the `pg_conn` fixture, `scripts/reconcile_stores.py`, and the integration tier — **all feature 10c**. Add **no** `@pytest.mark.integration` test here and open **no** connection anywhere in this feature.
- Any database-provisioning story: no Docker, `docker compose`, `testcontainers`, `initdb`, server-install or server-start instructions, no CI service container.
- Mirroring feature 10b's pending-decisions table into Postgres; any edit to 10b's module, migration, or tests; any edit to `tests/test_schema_parity.py` or its shared-table literal.
- Rewriting any shipped migration to remove its `DROP TABLE ... CASCADE` block; any `db/schema.sql` type change (feature 2 owns those); down-migrations or rollback (forward-only).
- Connection pooling, ORM, async, or any vendor SDK. One driver, one `connect()`; `supabase==2.9.0` stays unimported.
- Inventing an in-tree producer for any `disposition` CHECK value — the mapping covers the terminal set regardless of what emits those values today.
- Touching feature 10's Postgres-token grep-guard, or adding any call site into feature 10's shipped modules. 10c owns that edit.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)

The driver pin and the guard deletions must be staged and committed **together, in one commit**.
```