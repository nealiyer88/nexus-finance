## SITUATION

Read these before writing any code:
- `CLAUDE.md` (repo root) — test invocation rules, append-only log rules, branch policy
- `.claude/rules/01-nexus-finance-v1.md` — V1 architectural guardrails. **NOTE: §12 of that file is STALE.** It claims `core/matching/engine.py` is `[PLANNED]` / "DOES NOT EXIST". That is wrong — the file is shipped and is the Stage 6 dispatcher. Trust the repo over §12, and do **not** edit the rules file as part of this feature.
- `core/graph/pg.py` — feature 10a's shipped module. Read it for the real symbol names and the actual dotenv-fallback behaviour before relying on any description below.
- `core/graph/dispositions.py` — 10a's `MAPPING`.
- `core/graph/resolution.py` — feature 10's shipped Stage 6 module, which you will edit.
- `core/matching/engine.py` — read only, for caller context. Do not modify.
- `scripts/migrate_pg.py` and `db/migrations/postgres.manifest` — 10a's runner and manifest.
- `db/schema.sql` and `db/schema_sqlite.sql` — Postgres and SQLite DDL.
- `tests/test_resolution.py` — contains the grep-guards you must not break.
- `tests/test_migrate_pg.py`, `tests/test_dispositions.py`, `tests/test_pg.py` — 10a/10b shipped tests you must keep green and must extend rather than duplicate.

Skills: None.

**Current state (verified against the tree):**

- Feature 10a is SHIPPED: `core/graph/pg.py` exports `get_dsn`, `is_available`, `connect`, `BOOTSTRAP_TENANT_ID`, `PG_CONNECT_TIMEOUT_SECONDS`. `BOOTSTRAP_TENANT_ID` is annotated `str`.
- `pytest.ini` registers the `integration` marker with `--strict-markers`.
- `core/graph/dispositions.py` defines `MAPPING` as `{value: value for value in TERMINAL_SET}`, where `TERMINAL_SET` is parsed from the `pending_decisions.status` CHECK, located by globbing `db/migrations/*_sqlite.sql`. No filename or value literal appears there.
- Feature 10 shipped `core/graph/resolution.py`. `resolve_match` carries `tenant_id: Optional[str] = None` and commits SQLite at the end of its `try` block, after `add_alias` / `upsert_edge` / `store_training_pair`.
- Feature 12 shipped `core/matching/engine.py`. Its system-actor constant is a plain `str` passed as `approved_by=`; `tenant_id` flows through as `None` on the default path. This is why `actor_id` is written NULL and why writers fall back to `BOOTSTRAP_TENANT_ID`.
- Feature 10b shipped a SQLite-only pending-decisions migration occupying a numeric prefix with no Postgres counterpart.
- **The configured Postgres is reachable and is currently EMPTY** — `to_regclass` returns NULL for `schema_migrations`, `audit_log`, `approval_decisions`, `tenants`, `canonical_entities`. Server major version is ≥ 17, so `gen_random_uuid()` is built in and no `CREATE EXTENSION` step is needed. The first runner execution applies the whole manifest from scratch, and **the destructive-file guard will not fire naturally** — the test for it must plant its own precondition and remove it afterwards.
- **The dotenv correction is verified true.** With `DATABASE_URL` removed from the environment, `is_available()` returns **True** (the getter falls back to loading a gitignored dotenv file). With `DATABASE_URL` set to an explicit **empty value**, `is_available()` returns **False**. The getter imports the dotenv loader lazily *inside its own body*, so `monkeypatch.setattr` on the `dotenv` module attribute takes effect and there is no module-scope name inside `core.graph.pg` to patch. Re-confirm both by reading the module; do not trust this paragraph over the code.
- **Three** grep-guards live over `core/graph/resolution.py` in `tests/test_resolution.py`, not two: a Postgres-token regex guard, a bare-substring guard on the Signal-B3 ledger table name (the **plural** form of the word for a database transaction trips it; the singular does not), and an `UPDATE`/`DELETE` substring pair over the training-data table. Derive all of them by reading the guard tests themselves.
- `tests/conftest.py` does **not** exist — the `pg_conn` fixture is genuinely new, no collision.
- `tests/test_pg.py::test_no_secret_logging_paths` is parametrized over a **fixed** module list and will **not** auto-cover this feature's new modules. Its AST helpers `_emitting_nodes` / `_leaks_dsn` are a usable model for your own hygiene test.
- `.venv/bin/python -m scripts.migrate_pg --dry-run` exits 0. `.venv/bin/python scripts/migrate_pg.py --dry-run` raises `ModuleNotFoundError: No module named 'core'`. **The module form is the only working form.** `scripts/migrate_pg.py`'s module docstring still shows the non-working path-style usage — editing the runner is out of scope; do not "fix" it and do not copy that usage line into any new test, doc, or subprocess call.
- `tests/test_migrate_pg.py` already covers, **offline**: manifest-covers-disk, no-sqlite-manifest-entry, the prefix invariant, `--dry-run` opens no connection, fail-closed on a manifest entry naming a missing file, and the destructive-file guard against a fake connection. This feature needs the **live** equivalents — extend, do not duplicate, and keep the existing offline tests passing once the new migration lands.
- `tests/test_dispositions.py` already asserts `set(MAPPING.keys()) == TERMINAL_SET` and `set(MAPPING.values()) <= CHECK_SET`. That is 10a's shipped test — **do not "fix" it**. This feature's own new test is subset-only by design and does not conflict.
- Baseline full suite: green. `DATABASE_URL= ... -m "not integration"` currently collects the same count (no integration tests exist yet).

## TASK

Build the Postgres runtime tier: execute 10a's migrations live, add a tenant-bootstrap migration, ship `core/graph/tenants.py`, `core/graph/audit.py`, `core/graph/approvals.py`, and `scripts/reconcile_stores.py`, wire both writers into feature 10's Stage 6 path behind 10a's availability helper, and stand up the integration test tier (`tests/conftest.py`, `tests/test_pg_bootstrap.py`, `tests/test_audit_pg.py`) plus the no-database unit tests that prove the grep-level invariants.

## FILE PATHS

**Create:**
- `core/graph/tenants.py`
- `core/graph/audit.py`
- `core/graph/approvals.py`
- `scripts/reconcile_stores.py`
- `db/migrations/<next-free-prefix>_<name>.sql` — the tenant-bootstrap migration. **Compute the next free numeric prefix over EVERY file in `db/migrations/`, `_sqlite` siblings included.** Do not write a number or a filename from this prompt; derive it by listing the directory.
- `tests/conftest.py`
- `tests/test_pg_bootstrap.py`
- `tests/test_audit_pg.py`
- Additional no-database unit tests for the writers' grep-level invariants, the suppression proof, the signature check, the disposition subset check, and the secret-hygiene checks. Place them in new test files or extend existing non-owned ones — do not extend any file listed under DO NOT MODIFY.

**Modify:**
- `core/graph/resolution.py` — add the Stage 6 call site only. This is a deliberate cross-feature ownership edit.
- `db/migrations/postgres.manifest` — append the new migration filename **last**.

**DO NOT MODIFY:**
- `core/matching/engine.py`
- `core/graph/pg.py` (10a's dotenv fallback is shipped contract)
- `scripts/migrate_pg.py`
- `requirements.txt`
- 10b's pending-decisions SQLite migration, module, and tests
- `tests/test_schema_parity.py` (including its shared-table literal)
- `tests/test_resolution.py`'s grep-guards — none of the three may be retired, weakened, or edited
- `tests/test_dispositions.py`
- `db/schema.sql` (no type change; widening `actor_id` is a feature-2 change)
- Any shipped migration's `DROP TABLE ... CASCADE` block
- `.claude/rules/01-nexus-finance-v1.md`
- `TEMPLATE.md`, `.claude/settings.json`, anything under `.claude/hooks/` or `.claude/agents/`
- The developer's gitignored dotenv file — never move, rename, delete, or rewrite it, not in a test, fixture, or teardown

## CONVENTIONS

- Python 3.10+ via the project venv. `snake_case` functions, `PascalCase` classes, `UPPER_CASE` constants.
- **Every Python invocation is `.venv/bin/python -m ...`.** Bare `pytest` is not on PATH; `.venv/bin/pytest` does not put the repo root on `sys.path`. The migration runner is **always** `.venv/bin/python -m scripts.migrate_pg [...]` — in criteria, tests, docs, and any subprocess a test spawns.
- **All Postgres connections open through 10a's `connect()` and nothing else.** Do not import the driver to open a connection, do not read the environment directly, do not build a DSN from parts.
- **Hardcode no host, port, database name, or username anywhere** — not in a module, test, comment, fixture, docstring, or printed line. Assume no password component exists. Anything that would have to change when the DSN changes is a defect.
- **Provision nothing.** No Docker, `docker compose`, `testcontainers`, `initdb`, CI service container, or server-start instructions.
- **Absence of a configured DSN means "not available", never an error.**
- **"No configured DSN" ≠ "environment variable unset."** Two sanctioned suppression forms only:
  - *In-test (required wherever a test asserts unavailability):* `monkeypatch.delenv` the variable **and** `monkeypatch.setattr` the loader function on the `dotenv` **module object** to a no-op returning falsey, then assert the availability helper reports **not available** before exercising anything else. That assertion is mandatory — it is the proof the suppression worked.
  - *Command-level (whole-suite invocations):* set `DATABASE_URL` to an **explicit empty value** rather than removing it.
- **Treat `DATABASE_URL` as a live credential at all times, whether or not the value carries a password, and branch no code path on whether it does.** Never log it at any level, never `print` it, never let it into an error message (catch driver exceptions that embed connection parameters and re-raise scrubbed), never write it under `features/` or `.rocket/`, into a fixture, snapshot, log, migration, or committed config.
- **Do not put any guard-forbidden token into feature 10's shipped modules.** Derive the forbidden token set and covered path set **by reading the shipped guard tests' own regexes and path tuples at build time** — never transcribe them. The call site reaches Postgres **only** through 10a's availability helper and this feature's writer functions: one import from 10a, no driver import, no environment read, no DSN handling. Split-store and write-ordering rationale lives **only in this feature's new modules**; any unavoidable wording at the call site uses the singular form of the transaction word or unrelated phrasing.
- **Cite symbols, not coordinates.** No `file.py:NNN` references, no counts, no "exactly N" claims about the tree in any code, comment, test, or log. Every quantitative check is an invariant computed at build or test time.
- Never write a migration number or migration filename into a comment or doc; derive it.
- All log writes (`SHIPPED.md`, `DEBUG.md`, `RUN_LOG.md`, `PROMPT_LOG.md`, `CC-LEARNINGS.md`) are append-only. Feature branches only — never commit to main.

**Design contracts fixed by the brief:**

- `core/graph/tenants.py` — `resolve_or_create_tenant(conn, tenant_id, name, slug) -> str`. Takes and returns the id in `str` form, never a `UUID` object; the driver casts to the UUID column. `name` and `slug` are **required parameters with no defaults** (both columns are `NOT NULL`, `slug` additionally `UNIQUE` — verify by grep at build time). Implementation is `INSERT ... ON CONFLICT (id) DO NOTHING` plus `SELECT`. A `slug` collision on a *different* id must surface loudly as a clear error naming the colliding slug (never the DSN) — do not swallow it, do not retry it. Slug de-duplication is out of scope.
- **NULL-tenant fallback in every writer:** when the caller's `tenant_id` is `None`, use `BOOTSTRAP_TENANT_ID`; when it is not `None`, call `resolve_or_create_tenant`. Without this the default path FK-fails on every write.
- `core/graph/audit.py` — `log_resolution(conn, canonical_id, incoming_entity_raw, match_type, confidence, signals, category_pair, user_id, tenant_id)`. **INSERT-only**; the module contains no UPDATE and no DELETE path. `actor_id` is inserted as **NULL — mandatory, not optional**; the human-readable actor string is recorded inside the `audit_log.diff` JSONB payload under a stable key of your choosing. Do not coerce, do not hash into a UUID, do not add an actor column. Confirm the column list, that `diff` is JSONB, and which TEXT column holds the canonical identifier, by grep over `db/schema.sql` at build time.
- `core/graph/approvals.py` — `record_approval_decision(conn, ...)` writing `approval_decisions`, whose `disposition` column carries a CHECK. It imports 10a's `MAPPING` and adds no vocabulary of its own. No CHECK value, terminal value, or set literal is written into code or tests — derive by parsing.
- **Tenant-bootstrap migration:** `INSERT INTO tenants (id, name, slug) VALUES ('<BOOTSTRAP_TENANT_ID>', 'Bootstrap Tenant', 'bootstrap') ON CONFLICT (id) DO NOTHING;`, appended **last** in the manifest. Grep-assert at build time that `tenants` is **not** named in the canonical-schema migration's `DROP TABLE ... CASCADE` block. When deriving the `tenants`-referencing columns from `db/schema.sql`, match on the `REFERENCES ... tenants` clause — **the DDL aligns types with multi-space runs, so any pattern assuming single spaces finds nothing** — and **do not assert a count** of referencing columns.
- **Write ordering:** Postgres audit row **first**, then approval decision, then feature 10's SQLite graph mutation. The call site belongs **before** `add_alias`, inside `resolve_match`'s existing `try`, so a connect timeout propagates **before** any SQLite mutation rather than silently skipping past it. Re-drive safety comes from feature 10's existing idempotency on `(canonical_id, value, source)` and `(source_node, target_node, relationship)` — this is not atomicity.
- **`tests/conftest.py`'s `pg_conn` fixture:** `pytest.skip` with an explicit reason when the availability helper is `False`; the reason names the variable `DATABASE_URL` and never its value. Yields a connection wrapped in a transaction and **rolls back at teardown** so tests leave no residue. This is the default for writer, tenant-provisioning, and reconciliation tests.
- **Runner-execution tests are rollback-exempt by construction** — the runner opens its own connection and commits, so nothing it does is visible on the fixture's connection and nothing the fixture rolls back can undo it. Those tests use their own connection with autocommit semantics and **commit-then-clean**: capture the relevant object and row state before the run, then restore the database to what it found by explicit cleanup in a teardown that runs **even on failure**. **Hardcode no table name in the cleanup path** — object names come from the manifest, from `information_schema`, or from the runner's own output, computed at test time. The exempt set is: runner exit-0 plus the manifest comparison, the SQLite-dialect check, the fail-closed pair, runner idempotency, the destructive-file guard, `--dry-run`, and the post-runner bootstrap-tenant assertions. Everything else stays on rollback.
- **First-run reality:** no criterion may assume pre-existing tables or a pre-existing migration record. Each exempt test **establishes the precondition it needs** (seeding a row, dropping a record, planting a file) and removes it again afterwards.
- **`scripts/reconcile_stores.py` — the invariant is COVERAGE, not count equality.** Do **not** compare SQLite row counts against `audit_log` row counts for equality; that is false by construction. For a given tenant and a watermark timestamp (default: the earliest `audit_log.created_at` for that tenant): collect the distinct canonical identifiers in SQLite `entity_aliases.canonical_id` and `entity_edges.source_node`/`target_node`, restricted to rows at or after the watermark and scoped to the tenant by **joining to `canonical_entities.tenant_id`** (neither alias nor edge rows carry a `tenant_id` of their own — derive the join from `db/schema_sqlite.sql`). **A SQLite row whose `tenant_id` is NULL belongs to the bootstrap tenant for reconciliation purposes** — a plain equality join selects nothing and the comparison would "pass" on an empty set. Compare identifiers as text on both sides. **Normalize timestamps across engines** (SQLite TEXT `CURRENT_TIMESTAMP` format vs Postgres `TIMESTAMPTZ`) to a common UTC representation before comparing. **NON-VACUITY GATE:** before drawing any conclusion the script asserts its collected SQLite identifier set is non-empty; an empty set is a failure with an explicit message, never a silent success and never a zero exit. Exit non-zero **only** on the uncovered-mutation direction, printing each uncovered identifier. The reverse direction (audit rows with no graph mutation) is expected and benign — report as an informational line, do not fail. Print no DSN on any path.

## ACCEPTANCE CRITERIA

*Cross-feature ownership and the default no-database path:*

1. **Feature 10's grep-guards survive and are not retired — BOTH of them:** the Postgres-token regex guard **and** the substring guard forbidding the Signal-B3 ledger table name over the same shipped modules **still pass unmodified**. A test derives every forbidden token (regex and substring alike) and every covered path from the shipped guard tests themselves and asserts no covered file matches any of them. Nothing — no regex, no substring, no path — is transcribed into this feature's code. Note the substring guard is tripped by the plural form of the word for a database transaction, so the call site avoids it. *(Reality-check correction: there are **three** such source-reading guards over `core/graph/resolution.py`, not two — the derivation must sweep all `test_*` functions in `tests/test_resolution.py` that read `resolution.py`'s source, which picks up the third automatically.)*
2. **Under a dotenv-suppressed invocation** (`DATABASE_URL` set to an explicit empty value — never with the variable merely removed, which the dotenv fallback defeats), the added Stage 6 calls are no-ops and Stage 6 behaves as feature 10 ships it: `DATABASE_URL= .venv/bin/python -m pytest tests/test_resolution.py <orchestrator suite> -x --tb=short` passes with a collected count greater than zero. **Derive the orchestrator suite's filename by listing `tests/` at build time** — no literal is written here so it cannot rot. *(Reality-check note: `tests/test_disposition.py` and `tests/test_dispositions.py` both exist; the derivation must not confuse them.)*
3. **A no-database unit test proves the suppression itself, in-test:** it removes `DATABASE_URL` from the environment via `monkeypatch.delenv` **and** neutralises the `dotenv` module's loader function via `monkeypatch.setattr` to a no-op, then asserts 10a's availability helper reports **not available** and that the Stage 6 path performs no write. The not-available assertion is what makes the criterion verifiable. The test moves, renames, or deletes no dotenv file and edits no 10a module.
4. `DATABASE_URL= .venv/bin/python -m pytest tests/ -m "not integration" -x --tb=short` exits 0 under that same dotenv-suppressed form **and reports a collected count greater than zero**, parsed from the same run's output. Separately, `.venv/bin/python -m pytest tests/ -x --tb=short` reports a collected count **greater than or equal to** the count from the same command on the prior commit — both captured at build time from the runs themselves, with no literal count written into any test.
5. `git diff` for this feature's commit touches neither `core/matching/engine.py`, nor `requirements.txt`, nor 10b's module/migration/tests, nor the parity test — asserted from the commit's changed-path set.

*Migration execution (requires the reachable Postgres; every criterion in this block is rollback-exempt and runs on the commit-then-clean path):*

6. `.venv/bin/python -m scripts.migrate_pg` exits 0 with `DATABASE_URL` set, and `SELECT to_regclass('public.approval_decisions')` and `SELECT to_regclass('public.audit_log')` both return non-NULL.
7. **`schema_migrations` equals the manifest, derived at runtime — no hardcoded total.** The test parses the manifest (stripping blanks and `#` comments) into a list and asserts `SELECT filename FROM schema_migrations ORDER BY applied_at` returns that same list in that same order and nothing else.
8. **SQLite-dialect migrations are provably not applied:** `SELECT COUNT(*) FROM schema_migrations WHERE filename LIKE '%\_sqlite%'` returns zero, and the runner's stdout mentions no file matching `*_sqlite.sql` (neither `applied` nor `skipped`) — the filename set derived by globbing, never named. This explicitly covers 10b's migration.
9. **Manifest covers every Postgres migration on disk** (set comparison at test time), and the **prefix invariant** holds: no two files in `db/migrations/` share a numeric prefix unless one is the other's `_sqlite` sibling.
10. **Fail-closed both ways:** a syntactically-invalid `.sql` file dropped into `db/migrations/` without a manifest entry leaves the runner exiting 0 with `schema_migrations` still equal to the parsed manifest; and with a manifest entry naming a nonexistent file the runner exits **non-zero**, names the missing filename, and applies nothing.
11. **Runner-level idempotency (the only idempotency claimed):** a second immediate run exits 0, prints `skipped` for every manifest entry and `applied` for none, and leaves `SELECT COUNT(*) FROM schema_migrations` and a pre-seeded `SELECT COUNT(*) FROM canonical_entities` unchanged from the values captured after the first run (compare before/after, never against a literal). Seed a `canonical_entities` row before the second run and assert it survives.
12. **Destructive-file guard:** on a database where `canonical_entities` exists but `schema_migrations` holds no row for the canonical-schema migration, the runner exits **non-zero**, prints a message naming that file, and applies nothing (row count unchanged from the value captured before the attempt). *(Reality-check note: the target database starts empty, so this precondition does not arise naturally — the test must plant it and remove it afterwards.)*
13. `.venv/bin/python -m scripts.migrate_pg --dry-run` exits 0 and creates no table.

*Tenant provisioning (the post-runner seed assertions are rollback-exempt; the `resolve_or_create_tenant` and collision criteria run on the rollback fixture):*

14. After the runner completes, `SELECT COUNT(*) FROM tenants WHERE id = <BOOTSTRAP_TENANT_ID>` returns a row and the seeded id, read back **as text**, is byte-identical to `core.graph.pg.BOOTSTRAP_TENANT_ID` (a `str`), asserted in the test rather than eyeballed.
15. **A dependent insert referencing it succeeds:** an `audit_log` insert using `BOOTSTRAP_TENANT_ID` commits without an FK error, and the same row shape with a random unseeded UUID raises a `ForeignKeyViolation`.
16. `resolve_or_create_tenant(conn, <fresh UUID string>, name=..., slug=...)` returns that same value **as a `str`, equal by `==` to the argument** (not a `UUID` object), creates a `tenants` row where none existed, and a second call with the same id creates none and returns the same value (compare row counts before/after, not against a literal).
17. **Signature matches the schema:** a no-database test asserts `resolve_or_create_tenant` has no default for `name` or `slug`, and grep-asserts against `db/schema.sql` that both columns are `NOT NULL` and that `slug` is UNIQUE.
18. **Slug collision is loud:** calling `resolve_or_create_tenant` with a fresh `tenant_id` but a `slug` already held by a different row raises a `UniqueViolation` (or a wrapper naming the colliding slug) rather than returning silently, and the error message contains no DSN value. **NULL-tenant fallback:** a writer called with `tenant_id=None` inserts a row whose `tenant_id` equals `BOOTSTRAP_TENANT_ID` and does not raise.

*Writers:*

19. `core/graph/audit.py` exists; `log_resolution` inserts one `audit_log` row per call — asserted as a before/after row-count delta equal to the number of calls the test makes, with `category` and `tenant_id` populated (integration). `grep -nE "\b(UPDATE|DELETE)\b" core/graph/audit.py` returns no match — append-only enforced in code.
20. **No code path binds a non-UUID string to `actor_id`.** (a) a no-database test greps `core/graph/audit.py` and `core/graph/approvals.py` and confirms every `actor_id` binding is `None`/SQL `NULL` — no parameter carrying the caller's actor value is ever bound to that column; (b) an integration test calls `log_resolution` with a deliberately non-UUID actor string and asserts the insert succeeds, `actor_id` reads back NULL, **and the actor string is present in that row's `diff` JSONB payload** — read back from the database, not from the argument.
21. `record_approval_decision` inserts an `approval_decisions` row whose `disposition` satisfies the table's CHECK, for every key in 10a's `MAPPING`. A no-database unit test re-derives the CHECK set by grep over `db/schema.sql` and `TERMINAL_SET` from 10b's pending-decisions SQLite migration (located by listing `db/migrations/`, never by filename) minus the non-terminal value, then asserts `set(MAPPING.values()) <= CHECK_SET` **and** `set(MAPPING.keys()) <= TERMINAL_SET`. Both are subset assertions; **no set equality is asserted** and no set appears as a literal. *(Reality-check note: `tests/test_dispositions.py` already asserts the stronger equality form. That is 10a's shipped test — leave it alone; this feature's own test is subset-only by design and does not conflict.)*
22. `scripts/reconcile_stores.py` exits 0 on a healthy tenant **whose SQLite alias and edge row counts deliberately exceed its `audit_log` row count** (drive at least one resolution end to end) — proving coverage, not count equality. **Non-vacuity is asserted in the same run:** the compared identifier set is non-empty, read from the script's own output, and a run whose set is empty (including one where NULL-tenant SQLite rows failed to map to the bootstrap tenant) **exits non-zero and fails this criterion** rather than passing. A separate case forces the empty-set condition and asserts the non-zero exit. It exits non-zero with a per-identifier diff line when a mutated canonical identifier loses its last audit row, and exits 0 with an informational line only when an audit row exists for a resolution whose graph mutation did not land. Timestamps are compared after cross-engine normalization. (integration)

*Secret hygiene (scoped to this feature's own diff):*

23. **The DSN never reaches logs or errors:** a grep over `core/graph/audit.py`, `core/graph/approvals.py`, `core/graph/tenants.py`, and `scripts/reconcile_stores.py` finds no path that logs, prints, or formats `get_dsn()`'s return value or the raw environment value into a message. A unit test monkeypatches `DATABASE_URL` to a sentinel, exercises every failure path reachable without a database, captures stdout/stderr and the exception messages, and asserts the sentinel appears in none of them. *(Reality-check note: the shipped `tests/test_pg.py::test_no_secret_logging_paths` is parametrized over a fixed module list and will not auto-cover these four — write this feature's own coverage, modelling it on the shipped `_emitting_nodes` / `_leaks_dsn` AST helpers.)*
24. **The secret is not in the diff:** a check resolves the DSN at run time and asserts the whole value, plus each **covered component** it happens to carry, appears nowhere in this feature's commit diff and in no file under `features/` or `.rocket/`. **The covered components are exactly: the hostname, the database name, and the password if one is present — and nothing else.**
    - The URL **scheme** and the **port** are deliberately EXCLUDED, and excluding them is mandatory: neither carries a secret, both appear in every valid connection string including the sentinel DSN this feature's own tests construct, and both already occur on a clean checkout in the example-environment file. A check covering them would forbid its own solution.
    - The DSN's **user component is EXCLUDED**, and the captured-test-output clause is deleted. In this environment that component is byte-identical to the developer's OS account name and therefore a path component of the repository's own absolute path; it already occurs in committed material under `features/` and `.rocket/` before this builder writes anything, and the test runner prints the repository root in its own header. A criterion covering it would go red on a flawless implementation.
    - **A component the value does not carry is skipped, and its absence is never an error.** The currently-configured value has an **empty password component**, so the check must handle a missing or empty password without asserting on it and without branching any production code path on whether a password exists. The check requires no component to be present, holds the resolved value only in memory, and writes it nowhere. When no DSN is configured it runs against the sentinel and still asserts absence.
    - The substance is unchanged and unconditional: **no real credential material** appears in this feature's diff, in any log line, in any error message, or in any file under `features/` or `.rocket/`.
25. **Scoped to this feature's own diff, not the whole repo.** Across the lines **this feature adds** under `features/` and `.rocket/` — added lines only — no added line assigns a value to `DATABASE_URL`. The criterion deliberately does not grep the repo: other briefs already contain illustrative `DATABASE_URL=` text on a clean checkout. The substance is unconditional: the connection string is **never logged, never placed in an error message, and never written to any file under `features/` or `.rocket/`**.

*The integration tier must actually have run (F3 gate):*

26. `DATABASE_URL=<from the configured dotenv> .venv/bin/python -m pytest tests/test_pg_bootstrap.py tests/test_audit_pg.py -m integration -x --tb=short` exits 0 **and** its collected count is greater than zero, asserted from the same run's output.
27. **This feature may NOT be marked SHIPPED while the number of integration-marked tests that actually EXECUTED is zero.** That number is read from the run's own collection-and-outcome output — integration-marked tests reporting a passed or failed outcome, i.e. collected-and-selected minus skipped and deselected. It is **never** compared against an expected total, a baseline, or any number written in this prompt; the only assertion is *strictly greater than zero*. An all-skip run, a fully-deselected run, and a zero-collection run each fail this criterion regardless of exit code. **A skip is not a pass.**

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Supplementary runs required by the criteria above (all from repo root, all in the module form):

```
DATABASE_URL= .venv/bin/python -m pytest tests/ -m "not integration" -x --tb=short
DATABASE_URL= .venv/bin/python -m pytest tests/test_resolution.py <derived orchestrator suite> -x --tb=short
.venv/bin/python -m pytest tests/test_pg_bootstrap.py tests/test_audit_pg.py -m integration -x --tb=short
.venv/bin/python -m scripts.migrate_pg [--dry-run]
```

## NON-GOALS

- Everything feature 10a owns: the driver pin, `core/graph/pg.py`, `pytest.ini`, the runner and manifest authoring, the disposition mapping, and the requirements-diff guard retirement. **This feature adds no dependency pin and retires no guard.**
- Any database-provisioning story — no Docker, `docker compose`, `testcontainers`, `initdb`, server install/start instructions, no CI service container.
- Migrating the graph store to Postgres. Every table in `db/schema_sqlite.sql` keeps its reads and writes on SQLite (derive that list by grepping its `CREATE TABLE` statements rather than asserting a count). This feature adds a *second* store for approval + audit records only.
- Mirroring 10b's pending-decisions table into Postgres, any back-fill of pending rows into `approval_decisions`, and any edit to 10b's module, migration, tests, or the parity test's shared-table literal.
- Any edit to `core/matching/engine.py`, and any change to feature 10's grep-guards.
- Rewriting any shipped migration to remove its `DROP TABLE ... CASCADE` block; widening `audit_log.actor_id` or any other `db/schema.sql` type change; down-migrations (forward-only).
- Slug de-duplication / collision resolution for `tenants.slug` — a signup/onboarding concern.
- Database-enforced append-only on `audit_log` (trigger / `REVOKE` / `RULE`) — V1 enforcement is code-level. Row-level security — no `CREATE POLICY` exists anywhere and writes carry `tenant_id` as a value. Connection pooling, ORM, async, or any vendor SDK — `supabase==2.9.0` stays unimported.
- Inventing an in-tree producer for any `disposition` CHECK value; the audit middleware and audit dashboard page — feature 16.
- Adding a `sys.path` bootstrap to the shipped runner, or "fixing" its stale docstring usage line.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)