Writing the build prompt.

```markdown
## SITUATION

Read (in this order, before writing any code):
- `CLAUDE.md` — project conventions and the mandatory test invocation form.
- `.claude/rules/01-nexus-finance-v1.md` — architectural guardrails. Pay attention to §0 (BUILT vs PLANNED), §5 (confidence thresholds), §12 (owner map).
- `features/pipeline/resolution-graph-update.md` — the feature brief for this work (Stage 6 spec Section 9 shapes: the Decision / Graph Action / Training Data table, and the Idempotency Requirements section).
- `db/schema_sqlite.sql` — the ONLY schema created at runtime. Read the `CREATE TABLE` blocks for `canonical_entities`, `entity_aliases`, `entity_edges`, `system_references` and take every column name and CHECK constraint from here.
- `db/migrations/002_llm_training_data_sqlite.sql` — the `llm_training_data` DDL (columns: `call_id` PK, `tenant_id`, `category_pair`, `redacted_prompt`, `prompt_sha256`, `llm_response_json`, `created_at`).
- `core/graph/entity_store.py` — the read-only module you will extend. Read the module docstring (tenant-scoping paragraph) and the `get_aliases` / `are_clustered` implementations for the join-through-`canonical_id` tenant filter pattern.
- `core/matching/types.py` — the `Disposition` dataclass, `LLMAssessment`, and the `Action` `Literal` alias.
- `core/matching/llm_fallback.py` — read `llm_assess`, `_write_training_row`, and `_build_forbidden_tokens` by symbol name. These define the Stage 5 row you recover and the forbidden-token field set you mirror.
- `core/matching/redaction.py` — you use `leak_check` ONLY.
- `core/matching/indices.py` — `TokenIndex.build(conn, tenant_id=None)`.
- `tests/test_llm_fallback.py` — copy its `SQLITE_SCHEMA` / `TRAINING_MIGRATION` path-constant + `executescript` test-setup pattern verbatim.

Skills: None.

Current state (verified against the tree at build time):
- `core/graph/resolution.py`, `core/matching/training_data.py`, and `tests/test_resolution.py` DO NOT exist. Nothing from this feature is already done.
- `core/graph/entity_store.py` is read-only by convention: every symbol is a module-level query helper, `conn` first / `tenant_id: Optional[str] = None` last, no class wrapper, no shared state. `_neighbors` is the only underscore-prefixed module-level def. Its module docstring says "no write path. Stage 6 (resolution / graph update) will add write functions to this same module."
- `entity_aliases` declares `UNIQUE (canonical_id, value, source)`. `system_references` declares `UNIQUE (source, external_id)` — that key is GLOBAL, not per-canonical, so an unguarded upsert can silently re-point a reference across canonicals; scope the `ON CONFLICT` action accordingly.
- `entity_edges` has NO uniqueness constraint (only non-unique `entity_edges_source` / `entity_edges_target` indexes). Duplicate-edge prevention is a hand-written SELECT-then-branch inside the transaction.
- `entity_edges` declares `created_at` and has NO `approved_at` column, despite rules §4 naming `approved_at`. `INSERT ... approved_at` raises `no such column`. `tests/test_schema_parity.py` parity-checks `entity_edges` across `db/schema.sql` and `db/schema_sqlite.sql`, so adding the column would break it.
- `entity_edges.approval_count` is `INTEGER NOT NULL DEFAULT 0` — a first `upsert_edge` INSERT must set its initial value explicitly for the "+1 on re-confirmation" criterion to read cleanly.
- `canonical_entities` has a nullable `tenant_id TEXT`, plus `created_at`/`updated_at` defaults, and CHECKs `entity_type IN ('client','vendor','project','pl_unit','cost_center','contract','person')`, `entity_category IN ('organization','person')`.
- `entity_aliases`, `entity_edges`, `system_references` have NO `tenant_id` column. `llm_training_data` and `transactions` DO have their own `tenant_id` — do not generalize either way; read the DDL.
- `_write_training_row` in `core/matching/llm_fallback.py` ends with `conn.commit()`. Reusing it would commit a half-finished Stage 6 transaction. The upside: the Stage 5 `llm_training_data` row is already committed and visible to Stage 6 on the same `conn` regardless of Stage 6's open transaction.
- `llm_assess` sets `call_id = uuid.uuid4().hex`, `prompt_sha256 = sha256(redacted.text)`, persists `redacted_prompt=redacted.text`, and returns a `Disposition` whose `llm_assessment.call_id` is that same value. `category_pair` is persisted as `f"{a}:{b}"`.
- `core/matching/redaction.py`'s public surface is exactly `leak_check`, `redact_org`, `redact_person`. There is NO "render an entity pair through the scrubber" API, and Stage 6 does not hold the fixed positional structural arguments `redact_org` / `redact_person` require.
- `core/matching/llm_fallback.py` drops forbidden tokens shorter than `_MIN_FORBIDDEN_TOKEN_LENGTH = 3` (mirroring `redaction._MIN_TOKEN_LENGTH`). Apply the same ≥3 floor when re-deriving the token set, or a 2-char fragment will spuriously fire `leak_check` on a Stage 5 prompt that legitimately passed.
- `core/matching/confidence.py` and `core/matching/engine.py` do not exist. Thresholds live as module constants in `core/matching/disposition.py`.
- No Postgres anywhere: `grep -rniE "psycopg|DATABASE_URL" requirements.txt core/` returns nothing. `supabase==2.9.0` is pinned but never imported.
- `db/schema_sqlite.sql` currently holds five tables (four graph + `transactions`); migrations `003_transactions_sqlite.sql` and `003_transactions.sql` also exist. Tests load ONLY `schema_sqlite.sql` + `002_llm_training_data_sqlite.sql`.
- Fixtures available: `tests/fixtures/canonical_ground_truth.json`, `qb_entities.json` (46 records), `ruddr_entities.json` (45 records), `normalizer_expected.json`. There are no per-entity `ingest_*.json` files. `"pacrim tech"` is the normalized form of RUDDR-015 and appears in `canonical_ground_truth.json` and `ruddr_entities.json` as `pacrim-tech`.
- Baseline suite is green: 356 passed.
- `git show HEAD:core/graph/entity_store.py` succeeds today and resolves to a pre-feature commit — the derived-baseline test is mechanically possible.

## TASK

Implement pipeline Stage 6 (resolution + graph update) over the existing SQLite store: add six write functions to `core/graph/entity_store.py`, create `core/graph/resolution.py` and `core/matching/training_data.py`, and create `tests/test_resolution.py`.

## FILE PATHS

Create:
- `core/graph/resolution.py`
- `core/matching/training_data.py`
- `tests/test_resolution.py`

Modify:
- `core/graph/entity_store.py` — ADD the six write functions listed under CONVENTIONS. Update ONLY the "no write path" sentence in the module docstring. No existing function may change name, parameter order, parameter names, or defaults, and none may be removed.

DO NOT MODIFY:
- `db/schema_sqlite.sql`, `db/schema.sql`, `db/migrations/001*`, `db/migrations/002*`, `db/migrations/003*` — no schema change, no new migration file, no `approved_at` column.
- `requirements.txt` — `git diff requirements.txt` must be empty.
- `core/matching/llm_fallback.py`, `core/matching/redaction.py`, `core/matching/types.py`, `core/matching/indices.py`, `core/matching/disposition.py`.
- `tests/test_schema_parity.py`, `tests/test_deterministic.py`, `tests/test_blocking.py`, `tests/test_scoring.py`, `tests/test_disposition.py`, and every other existing test file.
- `TEMPLATE.md`, `.claude/settings.json`, anything under `.claude/hooks/` or `.claude/agents/`.

## CONVENTIONS

- Python 3.10+, run via the project venv. `snake_case` functions, `PascalCase` classes, `UPPER_CASE` constants.
- Feature branch only — never commit to `main`. All log writes are append-only.
- **Write-function signatures** added to `core/graph/entity_store.py`, module-level, `conn: sqlite3.Connection` first / `tenant_id: Optional[str] = None` last, mirroring the shipped read convention:
  - `create_canonical_entity(conn, canonical_name, entity_type, entity_category, confidence, tenant_id=None) -> str`
  - `add_alias(conn, canonical_id, value, source, category, confidence, tenant_id=None) -> int`
  - `add_system_reference(conn, canonical_id, source, category, external_id, external_fields, tenant_id=None) -> int`
  - `upsert_edge(conn, source_node, target_node, relationship, source_category, target_category, weight, approved_by, tenant_id=None) -> int`
  - `increment_approval_count(conn, edge_id) -> int`
  - `update_confidence(conn, canonical_id, new_confidence, tenant_id=None) -> None`
- **Tenant scoping, per write target:**
  - `canonical_entities`: `tenant_id` is a real column. `create_canonical_entity` writes it as a column value; `update_confidence` scopes with `WHERE canonical_id = ? AND tenant_id = ?` when a tenant is passed.
  - `entity_aliases`, `entity_edges`, `system_references`: NO `tenant_id` column. The `tenant_id=` parameter is a **scoping filter, never an inserted value** — verify the parent canonical is in tenant scope by joining through `canonical_id` (the pattern in `get_aliases` and `are_clustered`), or ignore the parameter. Never put `tenant_id` in an INSERT column list on these three.
  - `llm_training_data`: `tenant_id` is a real nullable column, written as a pass-through value.
  - `tenant_id` stays nullable everywhere. No `NOT NULL`, no FK to a `tenants` table (it does not exist in SQLite).
- **Idempotency:**
  - `add_alias` uses `INSERT ... ON CONFLICT DO NOTHING` on `(canonical_id, value, source)` and returns the existing `alias_id`.
  - `add_system_reference` upserts on `(source, external_id)`.
  - `upsert_edge` does a `SELECT` on `(source_node, target_node, relationship)` and, when a row exists, calls `increment_approval_count` instead of inserting a second edge.
- **Transaction boundary:** one `conn`, one `BEGIN`/`COMMIT`, rollback on exception, owned exclusively by `core/graph/resolution.py`. Neither `core/matching/training_data.py` nor the new `entity_store.py` write functions may contain `.commit(` or `.rollback(`.
- **`core/graph/resolution.py` API:**
  - `resolve_match(...)` — add alias, upsert-or-increment edge, mark indices stale, store training pair.
  - `create_new_entity(...)` — create canonical node with generated `canonical_id`, insert `system_references` rows, initialize edges to related canonicals, store negative training pairs for rejected candidates, mark indices stale.
  - `reject_match(...)` — no graph mutation; store a hard-negative training pair with the full signal breakdown.
  - `mark_indices_stale()` — returns a boolean staleness signal for feature 12's orchestrator. No incremental index mutation in this feature.
  - Input is the `Disposition` dataclass imported from `core.matching.types` plus a resolved human/auto decision.
- **`core/matching/training_data.py`:**
  - `TrainingPair` dataclass with exactly the fields `entity_pair`, `signal_breakdown`, `graph_evidence`, `category_pair`, `disposition`, `reasoning_trace`.
  - `store_training_pair(conn, pair, tenant_id=None) -> Optional[str]` writes at most one row to `llm_training_data` and returns the written `call_id`, or `None` when no row was written. It must NOT reuse `llm_fallback._write_training_row`; it issues its own `conn.execute(INSERT ...)` with the identical column list.
  - **LLM gate:** a row is written ONLY when `disposition.llm_assessment is not None` AND a Stage 5 `llm_training_data` row exists for that `call_id`. Otherwise: no row, return `None`. Auto-approved and NO_MATCH dispositions therefore produce no training row — specified behaviour, not a gap.
  - **Redaction:** call NO `redact_org` / `redact_person`. Recover the already-redacted Stage 5 text with `SELECT redacted_prompt, prompt_sha256, category_pair FROM llm_training_data WHERE call_id = ?` using `disposition.llm_assessment.call_id`, on the same `conn`. Call `leak_check` only, as a second-pass guard.
  - **Column mapping (fixed contract):**

    | Column | Value |
    |---|---|
    | `call_id` | `"resolution:" + sha256(f"{canonical_id}\|{incoming_entity_raw}\|{iso_timestamp}")[:32]` |
    | `tenant_id` | pass-through, nullable |
    | `category_pair` | `f"{source_category}:{target_category}"` (same format as `llm_fallback.py`) |
    | `redacted_prompt` | verbatim copy of the Stage 5 `redacted_prompt` for that `call_id` |
    | `prompt_sha256` | verbatim copy of the Stage 5 `prompt_sha256` for that row |
    | `llm_response_json` | `json.dumps({...}, sort_keys=True)` over the TrainingPair with `entity_pair` EXCLUDED |

  - **`llm_response_json` carries no raw entity pair.** Do not use `asdict(pair)` — it would serialize unredacted names/emails/IDs. The payload is the five non-identifying fields (`signal_breakdown`, `graph_evidence`, `category_pair`, `disposition`, `reasoning_trace`) plus `entity_pair_ref` = `{"canonical_id": ..., "source_call_id": ...}`. `entity_pair` stays on the in-memory dataclass and is used only to derive the `call_id` hash and the forbidden-token set.
  - **Leak check gates BOTH persisted columns.** Before the INSERT, build a `forbidden_tokens` frozenset from the raw `entity_pair` (normalized name, raw name, email, employee-id-like keys, canonical name and aliases — the same field set `_build_forbidden_tokens` walks, with the same ≥3-character floor) and run `leak_check` over `redacted_prompt` AND `llm_response_json`. If either fires, do not insert and raise — a leak is programmer error, matching Stage 5's inbound-leak behaviour.
  - **`disposition` vocabulary** is the `Action` `Literal` alias in `core/matching/types.py` (`AUTO_APPROVE` / `QUEUE_FOR_REVIEW` / `LLM_FALLBACK` / `NO_MATCH`) plus the terminal human outcome (`CONFIRMED` / `REJECTED`). Do NOT use the `approval_decisions` CHECK vocabulary (`approved`/`rejected`/`corrected`) — that table is Postgres-only and never runs.
- **Tests build schema from files, never Python DDL.** `executescript` `db/schema_sqlite.sql` then `db/migrations/002_llm_training_data_sqlite.sql` into an in-memory connection, following the `SQLITE_SCHEMA` / `TRAINING_MIGRATION` constants in `tests/test_llm_fallback.py`. Drive 10 entities from the existing `tests/fixtures/` files. No database server, no network, no `DATABASE_URL`.
- **This feature writes to exactly five tables:** `canonical_entities`, `entity_aliases`, `entity_edges`, `system_references`, `llm_training_data`. It must not read or write `transactions`.
- Cite symbols, not line numbers, in any comment or docstring you write — upstream features shift coordinates.

## ACCEPTANCE CRITERIA

1. `core/graph/resolution.py` exists and `from core.graph.resolution import resolve_match, create_new_entity, reject_match` succeeds.
2. **Read-signature preservation (derived, not enumerated):** a test *derives* the baseline public surface at runtime — it reads `git show HEAD:core/graph/entity_store.py`, `ast.parse`s it, and collects every module-level `FunctionDef` whose name does not start with `_` (private helpers such as `_neighbors` are excluded by that rule, not by a list). For each derived name it asserts the name is still importable from `core.graph.entity_store` and that `str(inspect.signature(fn))` equals the baseline signature rendered from the same AST. The test hardcodes **no count and no function list**; it fails on any rename, reorder, removal, parameter-name change, or default change, and passes unchanged when an upstream feature *adds* a function. If `git show` is unavailable the test fails loudly rather than skipping.
3. `.venv/bin/python -m pytest tests/test_deterministic.py tests/test_blocking.py tests/test_scoring.py tests/test_disposition.py -x --tb=short` passes unchanged (exit 0).
4. After `resolve_match` on a confirmed match: exactly one new `entity_aliases` row exists for `(canonical_id, value, source)`; `entity_edges` has exactly one row for `(source_node, target_node, relationship)` with `source_category`, `target_category`, `weight`, and `approved_by` all non-NULL and equal to the values passed in.
5. **Alias idempotency:** calling `resolve_match` twice with identical input yields `SELECT COUNT(*) FROM entity_aliases` unchanged after the second call, and the same `canonical_id` returned both times.
6. **Edge idempotency:** the second `resolve_match` leaves `SELECT COUNT(*) FROM entity_edges` unchanged and increments that edge's `approval_count` by exactly 1.
7. **Transaction atomicity:** a test injects an exception after the alias insert and before the edge insert; after the rollback, `SELECT COUNT(*)` on `entity_aliases`, `entity_edges` **and `llm_training_data`** each equals the pre-call value. Backed by a grep assertion that neither `core/matching/training_data.py` nor the new write functions in `core/graph/entity_store.py` contain `.commit(` or `.rollback(` — only `core/graph/resolution.py` may.
8. After `create_new_entity`: a `canonical_entities` row exists with a generated non-empty `canonical_id`, `entity_type` and `entity_category` passing the schema CHECK constraints, and one `system_references` row per supplied source.
9. **Index-rebuild visibility:** after `resolve_match` adds alias `"pacrim tech"` to a canonical id, rebuilding `TokenIndex` from the same connection and querying `"pacrim tech"` returns that canonical id. (Asserts the write is visible to a rebuild; incremental index mutation is out of scope.)
10. **Index staleness signal:** `from core.graph.resolution import mark_indices_stale` succeeds and `mark_indices_stale()` returns `True` after a `resolve_match` or `create_new_entity` call that mutated the graph, and `False` after a `reject_match` (which performs no graph mutation) with no preceding mutation in the same test. Asserted directly in `tests/test_resolution.py`.
11. `core/matching/training_data.py` exposes `TrainingPair` with exactly the fields `entity_pair`, `signal_breakdown`, `graph_evidence`, `category_pair`, `disposition`, `reasoning_trace` — asserted via `dataclasses.fields()`.
12. **LLM-gated capture:** given a `Disposition` that went through Stage 5 (a real `llm_assessment` whose `call_id` has a `llm_training_data` row), `store_training_pair` inserts exactly one row whose `call_id` starts with `resolution:`, whose `category_pair` matches `^[a-z_]+:[a-z_]+$`, and whose `redacted_prompt` and `prompt_sha256` are byte-identical to the Stage 5 row's; and `sha256(redacted_prompt).hexdigest() == prompt_sha256`.
13. **No LLM prompt → no row:** given a `Disposition` with `llm_assessment is None` (e.g. an `AUTO_APPROVE`), `store_training_pair` returns `None` and `SELECT COUNT(*) FROM llm_training_data` is unchanged. Same for an `llm_assessment` whose `call_id` has no Stage 5 row.
14. `llm_response_json` round-trips through `json.loads` to a dict whose keys are exactly `signal_breakdown`, `graph_evidence`, `category_pair`, `disposition`, `reasoning_trace`, `entity_pair_ref` — and **does not** contain the key `entity_pair`.
15. A rejected match **that went through the LLM** produces a `llm_training_data` row with `disposition == "REJECTED"` inside `llm_response_json` and a non-empty `signal_breakdown`.
16. **Privacy covers both persisted columns:** for a person-entity pair, neither the stored `redacted_prompt` nor the stored `llm_response_json` contains any of the input name, raw name, email, or employee_id substrings (case-insensitive). Asserted directly against the two column values, and independently via `leak_check(value, forbidden_tokens) is None` for each, using the shipped `core/matching/redaction.py`.
17. **Leak aborts the write:** a test forces a forbidden token into the payload; `store_training_pair` raises and `SELECT COUNT(*) FROM llm_training_data` is unchanged.
18. `training_data.py` and `resolution.py` contain **zero** occurrences of `UPDATE llm_training_data` or `DELETE FROM llm_training_data` — grep-level assertion in the test file. Training capture is append-only in code.
19. `grep -rniE "psycopg|DATABASE_URL|postgres" core/graph/resolution.py core/matching/training_data.py core/graph/entity_store.py` returns no matches; `git diff requirements.txt` is empty.
20. `.venv/bin/python -m pytest tests/test_resolution.py -x --tb=short` passes (exit 0) with `DATABASE_URL` unset and no Postgres running.
21. `.venv/bin/python -m pytest tests/ -x --tb=short` passes (exit 0) with no regression to the shipped suite.

Additionally, `grep -rn "transactions" core/graph/resolution.py core/matching/training_data.py` must return nothing (this feature does not touch the `transactions` table).

## TEST COMMAND

```
.venv/bin/python -m pytest tests/ -x --tb=short
```

Bare `pytest` is NOT on PATH, and `.venv/bin/pytest` fails to put the repo root on `sys.path` — every `from core...` / `from connectors...` import then fails to collect. Always use the `python -m` form.

## NON-GOALS

- **All Postgres work** — the driver pin, `DATABASE_URL`, `core/graph/pg.py`, `scripts/migrate_pg.py`, a `pytest.ini` registering `@pytest.mark.integration`, and the `approval_decisions` + `audit_log` tables. That is feature 10a (`features/infrastructure/postgres-store-bootstrap.md`), which depends on this feature, not the reverse. This feature adds zero entries to `requirements.txt` and reads no new environment variable.
- Durable append-only audit log / the `audit_log` table — 10a. Provenance here is carried by `entity_edges.approved_by`, `entity_edges.approval_count`, `entity_edges.created_at`, `canonical_entities.created_at`/`updated_at`, plus the append-only `llm_training_data` row per decision.
- Approval-decision recording to `approval_decisions` — 10a.
- Approval queue UI — feature 11.
- Confidence decay — feature 13.
- Write-back to source systems — Shadow Ledger only.
- Batch resolution (1000 entities at once) — V1 processes sequentially.
- Adding `approved_at` to `entity_edges`, or any change to `db/schema.sql` / `db/schema_sqlite.sql` / migration 001. No new migration file, no new DDL of any kind.
- Row-level security / `CREATE POLICY`. No query filters on `tenant_id` unless the caller passes one.
- Incremental index mutation in `core/matching/indices.py` — this feature only signals staleness.
- Migrating the SQLite graph store to Postgres.
- Building a new redaction path — no new `redact_org` / `redact_person` calls; `leak_check` only.

## EXECUTION

You are running UNATTENDED via `claude -p`. There is NO human in this session.
NEVER ask for confirmation, acknowledgment, or a decision — pick the safe default and
proceed. Execute every step end-to-end: build, write tests, run the test command until
green, then commit. Do not stop until the deliverable is complete or you hit the
3-attempt failure rule. (Quality is gated downstream: QA + code review must both PASS or
rocket halts for human review — so build correctly, but do not pause to ask.)
```